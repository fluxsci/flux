# V0.2.0 implementation evidence ledger

Started 2026-09-20 from `c289627` on `codex/v020-fortification` in `/tmp/flux-v020-fortification`.
The authorized scope is the five plans in the local `notes/major_v02_review/` handoff, all P0/P1 and accepted conditional P2. No release is published. Package completion requires its adverse as well as successful acceptance checks; implementation alone is not a completed package.

## Isolation and ownership

Original `/home/driessen2/flux` remains on main, its existing guide edit preserved. Existing port1420 server is untouched. Worktree carries a copy of handoff and guide edits. `test-results/v020/original-preservation.json` records 1,715 original tracked/build/library SHA-256 values for final comparison. Dependencies are initially shared read-only; dependency upgrades must create a separate install. Tests use disposable projects, HOME/config/cache/library, and a private display for native acceptance. Lighttable internals are excluded.

- Root: F01–F09, shared filesystem/native lease protocol, Figure/source publication, final integration/VR02–06.
- Paper/Slide: PS01–14, document sessions/export recovery, Slide identity and presentation.
- Native/References: NR01–12, close native side, canonical references/Reader, native capabilities/jobs.
- Shell/verification: VR01, SH01–08, shell checked transitions/ACK, shared manifest, runner/headless contracts.
- Shared contracts: F02 lease nonce, F01 complete snapshot, F03 accepted generation, F04 atomic editing, SH02 manifest intents, SH01+NR03 checked close, F06+NR04 asset/native capabilities, PS02 durable export recovery. Each has one implementation owner.

## Package status

All packages remain open until evidence below and the owner ledgers discharge their stated acceptance. Owner detail: [Paper/Slide](V020_PS_PROGRESS.md), [Native/References](V020_NR_PROGRESS.md), [Shell/verification](V020_SH_VR_PROGRESS.md).

### F02 — in progress

Implemented shared `electron/operationLease.cjs`, thin Node and sender-bound native adapters, opaque token release/renew, same-resource local queues, explicit parent-token nesting and automatic heartbeat. Short filesystem transitions use unique per-contender registers so stale cleanup cannot unlink a new operation's identity. A slow live local process is never expired by TTL. Native activity leases retain their parent while an explicit child operation runs; other senders and independent operations cannot borrow ownership.

Verification executed in isolated environment:

- `node --import tsx scripts/verify-operation-leases.ts`: PASS (same-process maximum concurrency1, explicit nesting, exception cleanup, stale-token release/renew refusal, live TTL, native two-sender and activity-child ownership).
- `node --import tsx scripts/verify-w3-locks.ts`: PASS (two-process library adds8/8, figure creates6/6, fast human deferral, bounded retry, external canvas preservation, enrich delta, all240 hot-contention increments).

Remaining: takeover/process-kill races, actual native handler integration, mutation lease checks before publication, consumer audit, compatibility and full suites. Local leases coordinate cooperating writers on one filesystem; they do not coordinate independently synced machines or sandbox trusted recipes.

## Qualification limits

No physical Wayland/NVIDIA display, macOS/Windows, signed/notarized packages, live institutional authentication, Microsoft Word, browser-store release or external paid agents have been qualified. These are not passes. Final run records must retain first failures and explain any fixture corrections. No timing threshold or fixture population may be weakened.

### Integration checkpoint — 2026-09-21 04:35 UTC

F01/F03/F04/F05/F06/F08/F09 remain in progress, with common Figure snapshot reads, staged reload adoption, exact-generation text recovery, preview capture tracking, atomic editor rollback, export independent of resident plot DOM, parser-backed passive SVG policy, import preparation ownership, and preservation records for explicit conflict overwrite now implemented. F07 geometry (beyond linear group expansion and guide copying) and VR02 release hardening remain open. Do not interpret the package list as completion.

Evidence (all root tests through `/tmp/run-flux-isolated.py`, private HOME/XDG):

- `verify-edit-transactions.ts`: PASS — throwing mutation and adapter callbacks restore model/history/redo/companion, nested commits are one undo, cancellation of a captured save remains dirty, active IDs/reload availability repaired.
- `verify-v020-figure.ts`: PASS — missing/corrupt/future/EACCES/EBUSY conversion matrix preserves every existing byte; duplicate identities/nonfinite authored values refused; actual Figure save paused after preview capture then cancellation + trailing save reopens restored width; ENOSPC at asset/canvas/caption/index rolls back exact prior text; three 61-node plots under100-node UI cache cap retain every override; passive SVG rejects network/active/namespaced/CSS-escaped resources and preserves local references/root style; single-quoted exponent physical dimensions match.
- `verify-figfiles-parity.ts`: PASS — identical GUI/Node models and saved bytes, legacy partial asset metadata, rotated cropped artwork/guides, caption/reference identities, save/reopen/no-op bytes, incomplete sibling refusal. Repaired invalid fixture palette (`colors`→actual `swatches`), invalid project manifest, absent actual asset bytes, Buffer slice bug, missing remove capability. Assertions/population retained.
- `verify-figsave-txn.ts`: PASS, all6 process-kill points retain index-last reference invariant. This test alone does not certify multi-file source recovery.
- `verify-undo-budget.ts`: PASS, retained31/50 ~2MiB snapshots at62MiB, correct floor/tip, over-budget single level retained.
- `figenh-13-partstyle.ts`: PASS, passive normalization idempotent, use inheritance, semantic/group overrides preserved.
- Broad intermediate pure run: **208/248 passed,40 failed**. Full first attempts retained in `test-results/runs/2026-09-21T04-29-24-110Z-18` and `/tmp/flux-v020-pure-first.log`; failures assigned by owner. This run predates subsequent fixes and is not final acceptance.
- `npm run check`:0 errors,1 existing separator accessibility warning at this checkpoint. Headless check has1 remaining Paper DOCX byte-buffer typing error assigned to PS (subsequently reported repaired; requires rerun).
- Compatible dependency refresh through declared semver ranges plus required CSS parser and DOCX XML parser, from separate reproducible install `/tmp/flux-v020-dependencies-2`. Latest audit reported0 findings. Before/after raw audits `/tmp/flux-v020-audit-before.json`, `/tmp/flux-v020-audit-after.json` (the latter interim5 findings before final transitive refresh). Updated dependency behavioral qualification remains open.

Native/browser tests run against isolated worktree `http://127.0.0.1:1421/` to preserve owner's existing1420 server. Concurrent implementation/HMR means intermediate browser results require a final frozen-source rerun. Physical platform qualification remains excluded from claimed passes.

### Integration checkpoint — 2026-09-21 05:40 UTC

The second intermediate pure cohort was241/253, with12 failures/blocks retained at `test-results/runs/2026-09-21T04-48-10-063Z-18`. The corrected-dependency preliminary UI cohort was93/106, with13 failures at `test-results/runs/2026-09-21T05-07-59-612Z-17`. HMR during concurrent implementation invalidated some seeded browser fixtures; these cohorts are diagnostic evidence, not final qualification. Source freeze and full reruns are pending.

New focused evidence:

- `verify-v020-crash-recovery.ts`: PASS, eight real SIGKILL barriers (durable journal, binary asset, semantic sidecar, canvas, caption, index, manifest, committed journal removal), exact six-file prior/new generation on restart, idempotent recovery, two real competing processes, and external-edit preservation. No manual deletion of stale locks in this gate.
- `verify-v020-controls-gui.mjs`: PASS, actual mounted ColorScaleControls and NumberField; unrelated notifications retain unsent range and error, target/source replacement resets; wheel target switch before220ms gives separate undo, destroy settles the owned session, Escape has no retained undo. `test-results/out/v020-controls.{png,json}` inspected. Component harness omits theme CSS; it tests lifecycle behavior, not final themed layout.
- `verify-v020-geometry-gui.mjs` (PS independent run): PASS,17 real pointer gestures including rect/text/path0/30/45/90°, rotated lines, flips/crop; preview matches committed pixels/bounds, one exact Undo, settled hover16.6ms and actual terminal double-click produces exactly three nodes. `test-results/out/v020-geometry-gui.json` and preview/pen PNGs retained.
- `verify-v020-conversion.ts`: PASS17 real filesystem cases including both-direction read fault matrices, unknown metadata, copied bytes, asset identity collision, registration-write rollback and stale-root refusal.
- Current Figure/source work now stages fresh project registration in the same v2 binary/text journal as composition and source bytes. Conflict records flush before replacement; changed assets without available bytes refuse. An actual Figure source GUI gate exposed source acceptance consuming an Undo; a dedicated accepted-source publication path now preserves the user history while rebasing source dimensions. Frozen rerun pending.
- A real host SVG pixel inspection exposed lost `<use fill>` inheritance. Shared preparation now carries inherited presentation attributes while preserving the referenced node's own declarations; host pixel rerun pending.
- Latest root renderer check0 errors0 warnings. Headless check was temporarily invalidated by NR's active fulltext change; NR reports that type repaired and check passing; final combined run remains required.

Release policy now verifies exact source/version/tag, all required cohort attempts, native resources before tests/build/package, fresh output directories, actual packaged app/CLI/MCP/encoder/correction/export/terminal smokes, and artifact SHA256. Publication consumes only a validator-generated exact NUL file list. A synthetic clean-Git three-architecture qualification gate passes adverse empty/missing architecture, stale source, blocked UI, modified artifact, duplicate artifact and absent physical approval cases; unrelated fixture ZIPs never enter its emitted list. No pipeline was run to publish a release. The main UI CI observer remains conditional on the documented five consecutive green **main CI** runs; this session cannot manufacture that remote evidence.

### Integration checkpoint — 2026-09-21 06:25 UTC

Production implementation now covers F01–F09, PS01–14, NR01–12 and SH01–08 within their conditional boundaries. Final qualification remains open below. Accepted P2 work includes exact-key bounded morph preparation, tested native family extraction/diagnostics, pure policy consolidation, and existing-control focus/keyboard lifecycle fixes. Broad Library/Reader view movement remains conditional on the controller contracts described in the NR ledger. F08 speculative manifest memoization is not enabled: mutation/invalidation ownership is not yet immutable; the palette-size product decision remains explicitly unresolved. No broad editor/history/framework rewrite, active SVG, telemetry, Lighttable internals, or release publication was introduced.

| Root package | Implementation and adverse evidence |
|---|---|
| F01 | Complete/partial/future/failed Figure snapshots; strict authored identity and numerical validation; both conversion directions preserve all prior files on refusal. `verify-v020-figure`, `verify-v020-conversion`, Figure/Slide parity and actual native journey. |
| F02 | Shared operation token leases and local queue; project→slides→manifest order, sender identity and publication assertions. Two-process240-increment contention, real stale contenders, killed writers and revoked ownership. |
| F03 | Recoverable binary/text generation includes source assets, semantic sidecars, canvas, caption, index, deck and fresh manifest registration. Eight SIGKILL boundaries and save/error/cancellation tests; baseline adoption only after publication. Returning Figure source catch-up runs against the newly accepted snapshot. |
| F04 | Exception-safe editor/history/companion rollback; standalone versus display-only mutations; preview cancellation remains dirty through a trailing save. Actual number-field target/teardown/Escape and ColorScale draft lifecycle. |
| F05 | Detached export prepares all required plot DOM despite editor eviction; missing bytes fail by asset name; prior valid targets survive failed native rendering. Vector structure, actual host pixels, PDF text/page geometry and exported SVG inspected. |
| F06 | Passive parser-backed SVG policy, CSS/local-reference preservation and XML escaping; per-project asset boundaries; actual Figure/gallery/Paper/portable hosts retain local glyph and root-style paint with zero unauthorized requests. |
| F07 | Hierarchical selection units preserve locked members and relative group placement; exact supported rotated resize/crop/line geometry; actual preview/commit/Undo/pen gestures and ≤100ms settled hover. No history budget change. |
| F08 | Prepared validated imports retain destination ownership across awaits, named mixed-batch failures and one Undo; Dissect bounded rows/chunks and cancellation; naming/preset checks and retained property drafts. |
| F09 | Explicit overwrite first stores exact external/accepted/editor recovery branches; future/unknown read failures refuse; actual conflict UI positive/negative actions and native failed-write/retry preserve bytes and dirty state. |

New exact evidence and corrections:

- Combined logic run `2026-09-21T06-15-34-230Z-16`: **264/266**, with source changes explicitly making the aggregate nonqualifying. Both failures retained: W2 expected missing-directory fsync errors to be silent, and figsave's second killed-child restart read a snapshot before the ordinary authoring recovery boundary. W2 now requires ENOENT (unsupported directory sync alone is tolerated); figsave invokes production startup recovery before accepting a snapshot and no longer manually deletes stale locks. Focused corrected runs `06-18-19-275Z-2` and `06-18-20-448Z-2` both PASS. Full frozen rerun is required.
- Actual filesystem testing exposed root-level generation parent `project.jso` instead of `.`. Fixed shared parent calculation and stopped both Node/native directory sync adapters swallowing ENOENT/EACCES/ENOSPC/EIO. `verify-v020-durability.ts` PASS: exact root parent, both adapters' fault behavior, revoked-owner refusal for bytes/timestamps/generated resources, retained recovery and successful new-owner retry.
- `verify-v020-export-recovery.ts` now executes real Node compilation and kills at five acknowledged phases: durable journal, first source rewrite, actual Pandoc filter execution, first restore and restoration complete. Fresh production CLI registry entry twice restores two Unicode LF/CRLF sources and mtimes, cleans the owned profile, preserves prior validated real DOCX, and refuses external edits/corrupt journals. Initial five-phase focused run PASS8.1s; current strengthened valid-prior-artifact version also passes in the broad logic run. Node compilation now uses the bounded process owner (30-minute deadline,4MiB retained log); non-success status cannot publish.
- Native scientific journey PASS8 in5.9s at `2026-09-21T05-52-49-418Z-16`: real keyboard/autosave/Undo, native decoded import, conflict recovery record, obstructed-file retry, both conversion directions, saved SVG and reopen bytes. The initial PNG exposed a returning-Figure stale-baseline source catch-up error; production corrected afterward and source GUI run `05-57-58-869Z-16` PASS. Final native rerun must include that correction.
- Actual worker backlog measurements now include eight trusted inputs while64 jobs remain pending per case: same-pane correction33.7ms, other-pane correction32.7ms, math33.1ms maximum;192 queued jobs settle. Ambient corrections are on and dual-pane ownership is exercised. These are private Xvfb/Mesa results, not physical-display qualification.
- Renderer check0 errors0 warnings and headless check0 at `06:14`; build PASS at `06:17` before final recovery-confinement review. Audit0 findings after reproducible compatible dependency install; raw before/after audits and disposition table retained. Documentation isolated render PASS20pages39files with Quarto1.7.32. A mistaken nonisolated docs attempt failed on the read-only owner cache; its log is retained and the successful run uses disposable HOME/XDG.
- An independent final review reproduced generic GUI generation recovery following a symlink from project A into simultaneously approved B. NR owns the narrowly scoped per-project validation/all-path preflight and regression; final build/package waits for that fix. Review also found Figure source save discarded its Slide lease handle. Root now checks both project/Slide ownership before preparation and every journal publication; actual bridge fault cases before preparation and after journal publication preserve all authored files and retain dirty edits/recovery. Focused Figure gate PASS at `06-23-08-139Z-2`.

Release-plan contradiction resolved from current production code: `flux://` is explicitly retired; current browser extension capture uses downloaded-file names and scoped native intake. Packaging exercises that installed capture contract and a neighboring-file preservation check rather than reviving the obsolete URL scheme. Fresh Linux deb/AppImage and packaged CLI/MCP/docs/native-helper/terminal/export/capture checks remain in progress. Signed/notarized macOS, both native Mac architectures, physical owner-display approval, live institutional sessions and real Word UI remain unavailable checks, not passes. Stable main-CI UI promotion still requires its five consecutive remote green runs.

### Acceptance audit and corrective wave — 2026-09-21 07:18 UTC

Frozen checks at06:36 passed **267/267 pure**, renderer0errors0warnings, headless0errors and production build. Frozen combined UI/UI-extra/Paper/Reader run `2026-09-21T06-38-03-428Z-16` finished **196/203**, `sourceChanged:false`. Seven original failures remain in that report: personal-library-dependent assignment fixture; ambient ink comparison across natural fading; snip save through disconnected module instance; W4/W5 fixture URLs losing `?fixture=demo` under alternate port; and direct-Electron capture/PDF starting on inherited Wayland instead of selected private display. No threshold or fixture population was reduced. Corrected W4/W5 saved-byte/retry assertions and both direct-Electron tests pass; ambient continuity now compares the same simulation time, separately retaining all active-animation latency measurements. Root's mounted fulltext progress/cancel gate passes6.0s, but its source-changing run is diagnostic.

An independent line-by-line acceptance audit reopened concrete gaps despite the broad passes. These are implementation obligations, not platform exemptions: NR06 early download/restart/aggregate-byte/strict ZIP behavior; NR08 PDF task setup and commit-time Reader source/context ownership; NR09 watcher candidates, bounded integrity repair and cold-build progress; SH01 owner-registry changes during a pending flush; SH03 readiness context notifications; PS03 captured DOCX figure/caption/root revision. Additional missing PS02/06/10/11/12/13 adverse/performance assertions are being added, including actual typing during held Quarto and worker failure/restart. Earlier package closure statements are superseded until these acceptance items pass. Owners retain the shared-contract boundaries above.

Current focused evidence: `verify-fulltext-refresh.ts`16PASS verifies300-paper cold progress, bounded warm work, immediate watched changes, missed-event repair, corrupt-index rebuild, events during refresh, per-pane progress/cancel and watcher coalescing; existing real-worker service12PASS. `verify-shell-owner-epochs.ts` passes delayed flush/new dirty owner, registration ABA and delayed-ready native context publication. `verify-v020-paper-contracts.ts` passes20 comparisons against the original unindexed correction oracle at5k/20k vocabulary and320 type/base apply-diff cases; `verify-v020-worker-lifecycle.ts` exercises worker construction/error/restart/stale replies and bounded queues/caches. `verify-v020-paper-lifecycle.mjs`55PASS covers actual dialog keyboard/focus, hidden/disposed ambient loops and drag teardown. Final combined build/cohorts remain required after this corrective wave.

Earlier fresh Linux AppImage/deb in `release/v020-local-2026-09-21T06-32-23.117Z/` both passed13 installed-shaped behaviors, including CLI/MCP, real encoder/PDF/terminal/capture/correction/fulltext and offline docs. They precede this corrective wave and are diagnostic artifacts, not the final build. The unchanged signed Firefox0.1.1 XPI does not contain hardened background source; the strict source/dist/signed-byte bundle gate correctly blocks release until maintainer re-signing. No signing, upload or publication is performed here.

### Corrective implementation checkpoint — 2026-09-21 07:48 UTC

The acceptance audit's implementation gaps are now covered by focused regressions; the final frozen cohorts below remain pending. NR10's explicit Reader snip-controller extraction and the native captured-DOCX revision test are the last active owner tasks.

- F01 missing index now requires positive empty inventory, including canonical canvas/asset/caption files, backups and manifest registrations; denied inventory cannot be bypassed by force. `verify-figure-missing-index.ts`13PASS. Fresh projects and indexed legacy projects remain supported.
- F02/F03 reference reconciliation uses a dedicated `figure-references` token lease spanning prepare through figure commit and manuscript reconciliation; an independent process cannot retire an active prepared journal. Cold document comparison/journal/write owns the manuscript lease and checks it again after the final read. Live Paper performs one editor transaction then its own leased autosave. Recovery preflights all real paths, rejects empty/malformed/unverifiable mapping evidence, preserves newer text and checks reference ownership before live mutation. Both Node/GUI adapters and actual `createFolder`/`addNote` recovery are exercised in `verify-figure-reference-safety.ts`27PASS; existing reference sync29PASS and identity gates remain green. The new Paper session GUI12PASS verifies captured-root acquisition, no baseline reads while waiting, retained late typing, cold writer conflict and explicit Keep mine.
- F06 semantic export's optional sidecars now distinguish ENOENT from malformed/unreadable/escaping metadata. `verify-render-manifest-confinement.ts`7PASS inspects actual group paint markup, valid internal links, explicit copied external-source compatibility, zero external reads through escaping links and preserved prior export bytes.
- NR09 resident fulltext refresh uses watcher candidates plus bounded128-item integrity repair, without a repeated full-library freshness stat sweep on every warm query. Index schema3 validates structure and a whole-payload integrity receipt; missing/corrupt/incomplete caches rebuild, inherited-property tokens retain exact hits, and indexed/phrase/short/full-scan missing-PDF status agrees. Progress arrives before1s and can cancel cold rebuild; per-pane stale completions cannot replace newer searches. Config changes await resident-worker termination before renaming roots. `verify-fulltext-refresh.ts`24PASS, real-worker service14PASS, mounted progress/cancel UI PASS6.0s. The earlier full300/1000/5000 oracle run passed24.1s (run07-18-52-551Z-16), with first progress24.0/26.8/50.1ms and max main-thread delay11.1ms; final rerun is required after the index-integrity followup.
- PS06 real browser paint36PASS covers nonlinear gradient stops, compound path holes and native curved path DrawOn/Change seeking. PS10 vocabulary oracle and all8-type optional-base apply/diff cases pass; PS11 worker constructor/error/restart/stale reply/cache/queue and stale bibliography-load regressions pass. PS12 actual keyboard/focus/ambient lifecycle55PASS. PS13 actual dense morph first-preview medians112.6ms→34.7ms (six runs, identical sampled path bytes, every cached run≤100ms); no timing thresholds reduced.
- NR06 actual extension VM now covers early completion, startup-owned cleanup, retry after startup failure, final-state deadlines, per-file/aggregate bytes and bounded run cancellation; bounded ZIP rejects malformed framing/CRC/declared size while accepting valid highly compressible data. NR08 native context persists across OS blur for external-agent reads, expires after lost owner heartbeat and is superseded by a newer focused native owner. Actual encrypted/invalid/zero-page PDF teardown and script-inert fixtures pass. Owner ledger retains exact counts and platform scope.

Final source/type/build/cohort/package evidence will supersede diagnostic runs only where it explicitly names an unchanged source identity. Existing signed Firefox XPI remains a known strict bundle blocker requiring maintainer re-signing; it is not silently replaced, weakened or claimed passed.

Verification selection audit: new focused pathMap entries initially shadowed existing first-match broad contracts (notably Slide player and Reader). Each new entry now retains the prior affected groups/pure safety floor and adds its focused test; Library retains legacy filters plus new World/fulltext behavior. `verify-changed-pathmap.mjs`90PASS retains the original authoring/source/ghost/inline-slide/performance selection assertions and an explicit expanded player selection. Native DOCX audit exposed a CRLF-vs-CodeMirror-LF preflight comparison; semantic comparison now normalizes line endings while the recovery journal still restores raw source bytes and mtimes.

### Source freeze — 2026-09-21 07:52 UTC

All four implementation owners are frozen. NR10 Reader snip operation extraction completed; reader-session63, capture27, World6, existing native-backed snip GUI22 and Assign9 pass. PS final retained-artifact Word8cases pass16.4s:24trusted keys max50.9ms, original3.25 caption/old bibliography/blue[67,133,190,255] in DOCX, live red99.25/late typing preserved separately, five actual kill phases and external-edit conflict exact bytes/mtime. Root independently opened the output PNG/native screenshot and parsed the saved DOCX XML. A bad caption fixture used `captions['']`; it now uses the real `__figure__` key and asserts persisted3.25 before starting export, retaining the failed attempt.

Final pre-cohort checks: `/tmp/flux-v020-final-renderer.log`0errors0warnings; `final-headless.log`exit0; `final-build.log`exit0; `final-docs.log`20pages39files with Quarto1.7.32; `final-audit.json`0 vulnerabilities. These names share the `/tmp/flux-v020-` prefix. Full source-frozen cohorts and fresh installed packages follow; no release qualification is claimed while signed Firefox/platform requirements remain unmet.

First clean committed cohort on `d80a690`: pure **275/276** at `test-results/runs/2026-09-21T07-53-21-866Z-22`. The sole failure was W12's structural assertion that `fsGuard` immediately follows the readdir handler opening; F01's new strict-mode validation intentionally precedes it. Product guard still precedes every filesystem access. W12 now checks that ordering and invokes the actual registered native handler for allowed project inventory, denied sibling in both modes and invalid options. Focused runner PASS (`/tmp/flux-v020-w12-fixture.log`). No production code or behavioral threshold changed; a full clean rerun follows on the test-fix commit.

### Frozen integration results and scale correction — 2026-09-21 08:38 UTC

Clean `7c6d160` (source digest `ae5c96dec822944062d69c6c56f9795c791fb9dd16335120679338b19a2cafee`) passed **276/276 pure**, run `2026-09-21T07-57-58-639Z-16`, and **213/213 combined UI/UI-extra/Paper/Reader**, run `2026-09-21T08-01-39-175Z-16`; both `sourceChanged:false`, no blocked/flaky attempts. Final integrated native held-Quarto artifact has24trusted keys max45.2ms, old3.25 caption/old bibliography and blue[67,133,190,255]; root read its saved DOCX XML and evidence JSON.

Bundle/startup/scale run `2026-09-21T08-29-10-965Z-16` was **14/16**, unchanged source: startup and10scale scripts passed, along with3bundle scripts. Signed Firefox background mismatch remains the expected external signing blocker. The other failure was actual Paper table typing: heavy p95 12.4ms/control1.9ms,6.2× against the unchanged6× ceiling (ratio denominator floors at2ms). Controlled repeat11.6ms/5.8× was too marginal to dismiss the failure. CPU profiling identified repeated protected-span membership and recursive Text line lookups in PS09's table scanner.

The sole production correction after the213-script cohort is a linear table traversal: sequential Text.iterLines, an ordered protected-span cursor and the existing pipe-header precondition. `parseAt` and grammar are unchanged. A frozen prior-traversal oracle requires exact models for241 real tables/360 excluded protected pseudo-tables, captions, absorbed rows, frontmatter starts and edited Text-tree offsets. Three exclusive scale runs now give cell p956.7/7.6/6.9ms, ratios3.35/3.80/3.45×, with the original20,000-line fixture,18-keystroke bursts and every threshold retained. Original failure/repeat/profile and three corrected runs live in `test-results/v020-paper-slide/scale-diagnostics/`. Full pure+Paper, rebuilt startup/scale and native acceptance follow on the next clean commit; the prior213-script result is specifically evidence for `7c6d160`, not misattributed to a newer revision.

Recorded limitation: the deliberately giant single paragraph containing20,000 unbroken lines still measures approximately189–191ms synchronous typing, as in the pre-correction run. The existing fixture records that parser ceiling separately from the gated realistically paragraph-separated scientific document. This work does not claim to meet100ms for that extreme case or replace the Markdown editor/parser architecture.

### Native acceptance correction — 2026-09-21 09:20 UTC

Clean `8b6223d` passed **313/313 pure+Paper**, run `2026-09-21T08-42-56-629Z-16`, unchanged digest `4f4e7c177b0f6788b04a6b85bbb73b1ab98f51eeed60bc42f96f7f63d3acf99c`. Saved DOCX XML/pixels were inspected again: captured3.25 caption/old bibliography/blue figure, later typing saved separately;24trusted inputs max45.0ms. The first bundle/startup/scale cohort (`08-52-01-117Z-16`) was14/16: signed Firefox plus a prose timing tail6.8ms/3.4×. Three unchanged exclusive repeats passed4.0/3.4/5.4ms;120-key profiling did not establish a concrete cause, so no speculative product change was made. The complete repeated cohort (`08-59-11-434Z-16`) passed15/16: all11scale/startup/3other bundle scripts, only signed Firefox failed. Prose4.9ms, citation9.2ms, cell8.2ms p95; paint≤33.5ms. The original unexplained prose failure remains a recorded timing limitation, not erased by the repeats.

The first full native cohort (`09-01-43-653Z-16`) passed18/22. Its four failures are retained. Cellpress was incorrectly marked offline and hung on absent disposable `keys.json`; proxy capture also requires explicit institutional access. Their metadata/preflight now reports **blocked** without network opt-in and an explicit disposable HTTPS proxy fixture; every live assertion remains, and the eight Cellpress helper assertions moved to a hermetic gate. Netget's actual local-server/cookie/redirect/SSRF cases passed unchanged. Gallery clicked its controls before checked Slide checkout was ready; a fixture-owned readiness wait retains all11assertions, with32.5/35ms image/video paint and identical saved bytes in the focused rerun.

The video fixture first exposed a nested Electron sandbox-setting loss; a test-only launch adapter preserves the actual executable and applies explicit private-display/sandbox flags across real CLI hops. Once actual capture ran, its second immediate export exposed a **product regression**: registry startup recovery unconditionally acquired project/slides/manifest leases, even with no recovery journal, colliding with the GUI's recent-human-activity lease after a successfully saved edit. `recoverProjectForAuthoring` now probes project-confined journals and only enters the normal leased recovery when one exists. Recovery re-reads under ownership; ordinary mutations still use their own leases. A dedicated CLI/MCP/human-lease/pending-journal/confinement regression and the native immediate-second-export/cancel/prior-MP4 assertions cover this change. Full frozen affected suites and fresh packages follow this correction; earlier results are not attributed to its new source revision.
