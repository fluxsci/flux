# V0.2 shell, verification, and Dissect progress

Updated 2026-09-21 UTC. Isolated branch `codex/v020-fortification`, checkout `/tmp/flux-v020-fortification`; original checkout and owner's :1420 server are untouched. Disposable execution configuration is allocated before module imports. This is implementation evidence, not a release or cross-platform qualification.

## Implemented

- VR-01: explicit per-script runtime/prerequisites/isolation/exclusivity metadata; runtime does not change with selector. Scratch HOME/config/FluxConfig and short `/tmp/fv-*` Chromium temp roots; unique run/attempt logs; spawn errors, signals, timeout, blocked prerequisites and flaky retries remain non-green. Direct Electron and Electron-as-Node contracts are distinct. Standalone release-stage scripts are explicitly blocked without coordinator arguments; unknown prerequisites also fail closed. Root owns central manifest registration after Wave 0.
- SH-01: serialized/coalesced checked shell transitions; superseded project loads cannot publish; dirty/failed/throwing owners retain their editors. Pane closes flush their own Paper/comments registrations. Native flush ACK includes request ID and saved/blocked result; native coordinator belongs to NR. GUI document mutations take manuscript then manifest leases and assert both before writes. Home clears project/terminal/feedback ownership.
- SH-02: shared validated fresh-manifest intent transaction, renderer and Node lease adapters, document/deck/reindex registrations. Supported unknown fields preserved; corrupt/future manifests rejected untouched. Document reorder has per-object optimistic revision rollback. Move/delete preserve immutable pre-operation UTF-8 bytes in `.meta/document-recovery/*.json`; failure restores removed files; no second success manifest write after a helper commits. Core policy error types preserved.
- SH-03: resident Figure/Slide ownership capture, bounded typed operation preflight, correct-tenant checked save and explicit applied-but-unsaved responses. Context follows root/focus/tenant. Loopback client validates metadata/root/session identity, rejects redirects, limits response bytes and deadlines. Native bridge session metadata added.
- SH-04: immutable feedback operation root/generation; late reads/captures ignored; cancelled capture URLs revoked; failed writes preserve draft and expose retry; explicit false append result is failure. Successful note followed by failed Send does not duplicate the note on retry.
- SH-05: terminal start/root/restart/kill generation and stale-PTY retirement; subprocess status distinguishes signals/missing executables/nonzero. Agent log drains before journal; stream errors/cancellation terminate owned process tree. Failed attend work remains retryable only on a new explicit Send. Packaged MCP uses fileURLToPath and current runtime. Native PTY write/resize/kill is restricted to the owning webContents; foreign calls cannot control a session.
- SH-06: strict maintained headless TypeScript project, CJS declarations, CLI coercion exhaustiveness, GROBID csv keys, declared flags/help and stable errors. Migrated library input, slide patch/animation, and full-text search wrappers into registry. Existing streaming/interactive, image-content, network-routing, aggregate and live adapters are inventoried with exact contract differences in [V020_REGISTRY_INVENTORY.md](V020_REGISTRY_INVENTORY.md). Every one of the 12 declared coercion paths is exercised through parser→handler; residual legacy booleans preserve s2 credential-value versus provider-flag semantics.
- SH-07: unknown-value preference decoding, recent filtering/dedup/cap, finite UI dimension bounds and supported migrations; mode import error shows local retry view.
- SH-08: focus action on palette/feedback/capture, accessible combobox selection, keyboard splitter 20–80%, complete pointer cancel/blur/unmount cleanup.
- VR-02 (delegated fetchers): shared streaming download deadline/exact pinned-byte/hash boundary; bounded decompression; complete cached file inventory rehash; ELF/Mach-O/PE architecture headers and POSIX executable bits; required libraries/backend/licenses; per-target shared operation lease; interrupted staging cleanup; last-good rename recovery. No network activity on verified cache hits.
- VR-03: dynamic project load/scaffold keeps heavy Figure/Slide/compile validation out of Home; production eager graph1119.0→575.8KB under unchanged800KB ceiling. Three cold browser contexts measure Home, first Paper, first Figure and first-keystroke separately with long-task records.
- F-08 (delegated Dissect portion): native bounded UTF-8 read caps IPC before decoding; cooperative parser retains at most5000 rows/128columns/4096chars per cell/2MiB total text and reports truncation. Incomplete byte-prefix records are not displayed as observations. Shared collator; cancellation and cache/list/table epochs prevent stale results and stale-finally deletion of newer work.

## Exact verification evidence

All directories below are under `test-results/runs/` and preserve attempt output and summary configuration.

| Check | Evidence/result |
| --- | --- |
| Renderer types | `npm run check`: 0 errors,0 warnings (05:00 check after all shell/Dissect/startup changes; later Feedback busy repair pending final root check) |
| Headless types | `npm run check:headless`:0 errors after actual snips lease assertion seam correction |
| Runtime adapter failure/isolation regression | `2026-09-21T04-36-48-610Z-2`: 1/1 pass |
| Fresh manifest concurrency/corruption/recovery bytes | `2026-09-21T05-20-19-406Z-2`:1/1 pass, injected ENOSPC restores document and preserves exact recovery/manifest bytes; actual GUI bridge create/delete asserts manuscript→manifest lease acquisition and ownership before every write |
| Document deletion policy and real CLI | `2026-09-21T04-36-45-253Z-2`:40 checks pass |
| CLI parser/GROBID/error/preferences | `2026-09-21T04-36-48-059Z-2`:1/1 pass |
| Real loopback client↔server command/context | `2026-09-21T04-36-44-633Z-2`:1/1 pass |
| Live dispatcher persisted bytes and failed ACK | `2026-09-21T04-44-22-796Z-2`:1/1 pass; actual saved caption, ENOSPC retained dirty caption, invalid numeric refusal, missing owner refusal |
| Figure parity fixture owner repairs | `2026-09-21T04-36-13-182Z-2`:16/17 initially; expected crop diagnostic restored and exact crop fixture passes `2026-09-21T04-38-13-552Z-2` without removing assertions |
| Registry CLI+MCP parity | `2026-09-21T04-36-46-886Z-2`:pass current migrated groups and real protocol session (42.4s) |
| Dispatch subprocess/log bytes | `2026-09-21T04-17-51-225Z-2`:pass including SIGTERM status and final100KB log |
| Live-client unresponsive/redirect/size failures | Direct `node --import tsx scripts/verify-live-client.ts`:pass; loopback only |
| Dissect200k rows, UTF8 caps, async stale requests | Direct `node --import tsx scripts/verify-dissect-bounds.ts`:pass, max32K-char scan1.91ms,5000-string-row sort1.32ms,203 cooperative timer ticks; strict100ms bounds unchanged |
| Shell real browser adversity | Direct `FLUX_URL=http://127.0.0.1:1421/ node scripts/verify-shell-fortification.mjs`:pass including superseded corrupt-open intent, controlled Library chunk failure/local retry with dirty Paper neighbor retained, small-screen reduced-motion failed feedback/retry/focus; screenshot `test-results/out/shell-fortification.png` visually inspected: both Paper buffers remain, separator focus visible, failed-save toasts visible |

| Runtime fetcher adverse regression | Direct `node scripts/verify-runtime-assets.mjs`:32 checks PASS; original and restored replacement bytes, truncated/offline/timed-out downloads, architecture/mode/license/library corruption, concurrent installer queue, interrupted staging cleanup |
| Actual Linux cached bundles | Read-only `verifyCorrectionRuntime` b10288:41 hashed files PASS; `verifyVideoEncoder` b6.1.1:binary/LICENSE/README/NOTICE PASS; no downloads or cache mutations |
| Correction runtime complete gate | `2026-09-21T05-19-46-704Z-70`:47/47 PASS; structural archive metadata check imports actual pinned constants and shared release policy; actual corruption behavior lives in runtime-assets |
| Startup production graph and cold UI | `/tmp/flux-v020-startup-cold-final.log` and `test-results/out/startup.json`:575.8KB; Home58.1/58.8/61.0ms; Paper102.8/104.2/118.0ms; first Figure70.0/75.8/81.2ms; first input5.8/6.0/7.6ms; maximum long task61ms; no browser errors. `startup-cold-figure.png` visually inspected after transition completed. |
| Small reduced-motion artifact | `test-results/out/shell-feedback-small-reduced-motion.png` visually inspected at800×600: error, retained draft, Add/send controls and focused textarea visible; previous deliberately generated failure toasts still visible |

## Remaining verification and limitations

Full Paper gate is required after the document changes and is coordinated by root/PS on isolated :1421 because the owner's mandated :1420 endpoint must not be disturbed. Vite fs.allow resolves the isolated dependency symlink; previous WASM403 was fixed from actual HTTP evidence. No native Windows/macOS or physical owner Wayland/NVIDIA check is claimed. No release is published. Root handles final frozen build/release CI and cross-package suites; startup implementation and focused measurements above are from this agent.

The first broad106-script UI cohort (`2026-09-21T04-57-37-384Z-16`) was interrupted after59 completed scripts when the dependency symlink changed and Vite retained the old allowlist. Its failed attempts and interruption record are retained. Restarting only the isolated :1421 service restored the exact WASM request from403 to200. The corrected preliminary cohort `2026-09-21T05-07-59-612Z-17` completed93/106 passing,13 failed. This run overlapped HMR source reloads and is not final qualification. Root owns Figure failures; PS owns tables/Slide failures; this agent owns citation/picker/Paper-files/render-lifecycle fixture diagnosis. Full frozen reruns remain required.

The UI cohort found a real Feedback busy-state regression after successful Add (fixed close reset) and unguarded Send rejection (now the same caught/retry path). Context and annotate now pass with injected false-send persistence assertions. The Paper-files fixture assumed Home retained an active project model; it now configures saved manifest bytes while closed and reopens normally, preserving its behavioral assertions. Citation, figure-picker and image-lifecycle fixtures now await the explicit initial Paper source readiness seam; PS removed redundant initial revision loads that could overwrite seeded state. Controlled reruns passed as recorded below. A direct manifest verifier import briefly attempted the real FluxConfig path after scaffold policy changed and failed ENOENT before any write; the verifier now creates scratch HOME/config before dynamic core imports. Runner executions were already isolated. Recovery records are retained deliberately; no automatic destructive replay or garbage collection was introduced. An ambiguous feedback append failure can leave its own PNG recoverable; no project-wide snapshot sweep occurs.


## Additional controlled evidence (2026-09-21 05:20 UTC)

- Runner cancellation: actual SIGINT run `2026-09-21T05-10-26-125Z-24` recorded interrupted status, signal, preserved attempt logs and exit130. Adapter regression waits fixture readiness before abort; no timing-based startup guess.
- Native PTY ownership: `node scripts/verify-terminal-ownership.cjs` passes registered-handler tests with two senders, rejecting foreign write/resize/kill and retaining owner delivery/cleanup.
- Dense Dissect actual UI in `2026-09-21T05-07-59-612Z-17/verify-dissect-gui.mjs/attempt-1/artifacts/dissect-dense.json`:200000 input rows;5000 retained;34 mounted DOM rows;38 animation frames while counting; worst frame16.8ms;5000-string-row sort14.5ms; correct sorted first subject195001. Existing100ms limits unchanged. Screenshot inspected; truncation/count/sort-window explanation moved outside the virtual spacer into a visible footer and visibility is asserted.
- Release evidence validator adverse suite: `node scripts/verify-release-policy.mjs` passes actual CLI against disposable synthetic clean Git fixture and three architecture reports. Valid output contains exactly six NUL-separated artifact paths and excludes an extra fixture ZIP. Missing architecture, empty artifacts, blocked UI, stale source digest, duplicate/corrupt artifact, and absent physical approval all reject. Synthetic reports are tests, not platform qualification.

- Controlled citation/picker/document checks after readiness repair: `2026-09-21T05-31-12-908Z-16` citation seven behavior booleans PASS (screenshot inspected); `2026-09-21T05-31-32-881Z-16` picker12 checks PASS; `2026-09-21T05-32-00-647Z-16` document-files29 checks PASS with actual saved paths/bytes and5000-document expansion maximum38.1ms.
- `2026-09-21T05-32-16-973Z-16` render-optimizations retained failure: server warning identified invalid synthetic Figure fixtures missing finite x/y. The same warning revealed that the earlier picker geometry pass did not establish thumbnail paint. Both fixtures now supply complete placement coordinates; picker additionally asserts first-thumbnail exact teal pixel, last-thumbnail decode and saves a rendered grid screenshot. Controlled reruns passed: render-optimizations24 checks at `2026-09-21T05-36-14-915Z-16` (36 input turns during dense preparation, identical SVG bytes, cancelled obsolete preview); figpicker14 checks at `2026-09-21T05-36-39-397Z-16` (exact teal pixel and first/last real thumbnail decode). The painted-grid screenshot was visually inspected. The production validation remains strict.

## Installed documentation completion (2026-09-21 05:50 UTC)

Root delegated the package-only documentation gap after an actual package review. `electron/documentation.cjs` resolves packaged help at `<resources>/docs/index.html` and source help at `<checkout>/docs/_site/index.html`, preserves native opener failures, and never opens a browser during verification. `electron-builder.yml` includes the generated site in every platform resource override. `build:docs` is a packaging prerequisite; ordinary `npm run build` remains unchanged. Release setup uses the [official Quarto setup action](https://github.com/quarto-dev/quarto-actions/tree/main/setup) with1.7.32, the version exercised locally. Root owns required release evidence and full package execution.

- `scripts/build-docs.mjs` renders20 existing QMD pages into a unique disposable stage under a shared operation lease; verifies every page and local HTML/CSS resource, rejects contributor/source leakage, writes a full SHA256 inventory and only then replaces the previous site. Render/missing-resource/failed-rename regressions retain exact old bytes and remove only the owned stage.
- Generated directory links become explicit `index.html` links for OS file browsers. Quarto1.7.32 explicitly refuses its usual search fetch under file URLs; the packaged site now preloads Quarto's existing Fuse index/options only for file URLs, preserving the normal HTTP path. A changed upstream search contract fails the build for review.
- Actual render evidence: `test-results/docs-build-2026-09-21T05-45-44-832Z`:PASS20pages,39files,Quarto1.7.32. Earlier normalization failure (`05-41-52-385Z`) is retained; corrected pre-search render (`05-43-22-638Z`) also passed. Real config and user files were never used.
- `node scripts/verify-documentation-package.mjs`:40 checks PASS, including synthetic macOS/Linux/Windows resource layouts, spaces/Unicode, missing installed docs refusal, source workflow, native opener errors, generated local navigation, offline index semantics and HTTP behavior. These platform paths are structural tests, not native execution claims.
- Actual headless Chrome opens a copied package resource layout in `Moved app Ω with spaces`, follows Home→Figure→Home, and returns seven semantic-search entries under file URLs with all HTTP requests blocked. No alerts or page errors. `test-results/v020-docs-file/result.json`, `home.png` and `search.png` inspected. Existing optional Google webfont falls back to readable system fonts offline; documentation content/navigation/search are local. No owner's OS browser was opened.
- Runner runtime regression rerun after explicit release-argument/unknown-prerequisite checks:PASS. Release publication adverse suite also now deletes required docs evidence and confirms actual publication validation refuses it; complete `verify-release-policy.mjs` rerun PASS.

All owned SH P0/P1, accepted SH08, VR01/03, delegated runtime fetchers, documentation packaging, and delegated Dissect implementation work is complete. No owned implementation blocker remains. Root's final frozen renderer/headless checks, full Paper gate and cross-package/platform qualification remain separate required final evidence; no release or unavailable platform pass is claimed here.

## Final runner provenance and native diagnostics (2026-09-21 06:13 UTC)

- Runner reports now capture source identity at start and end: commit, dirty state, exact content digest including untracked nonignored implementation files, and Git index metadata digest. Source changes retain every individual result but mark the aggregate nonqualifying and return failure. `verify-runner-runtime.mjs` exercises the real runner in a disposable empty Git repository with untracked source, both unchanged and deliberately changed during an otherwise passing child. `verify-release-policy.mjs` checks the same content identity and refuses changed-source summaries. Both focused checks PASS. DCONF_PROFILE=/dev/null and GSETTINGS_BACKEND=memory prevent GTK tests from consulting the owner's dconf store.
- TestProcessScope recognizes nested Electron CLI and direct binary launches and adds --no-sandbox only with explicit FLUX_ELECTRON_NO_SANDBOX=1. Native runner attempts provide a disposable executable FLUX_VIDEO_ELECTRON shim for production media workers; ordinary application argv/sandbox policy remain unchanged. Actual nested wrapper and executable-shim tests PASS, including a binary path containing a quote, spaces and Unicode, exact argv retention and no duplicate flag. Pure attempts do not require Electron merely because the opt-in is inherited.
- Declared prerequisites now verify real Quarto, Chrome and pdflatex/KOMA/lmodern/bookmark/longtable availability. Native encoder checks select the current platform/architecture, fully rehash pinned executable and ancillary inventory, validate architecture/executability and execute ffmpeg -version. Missing, invalid or unknown contracts block before an attempt. Local checks report all four prerequisites usable; this is Linux evidence only. CI test provisioning adds Quarto1.7.32, Chrome, the pinned host encoder and TeX packages before offline checks. [Quarto documents the supported PDF engines and external TeX distributions](https://quarto.org/docs/output-formats/pdf-engine). Hosted CI has not been claimed to run here.
- Accepted NR10 diagnostics now use a trusted local primitive with fixed family/outcome/error enums, an opaque operation UUID, timestamp, a 128-record/64KiB cap, bounded historical reads, owner-only atomic files, and best-effort failure behavior. Arbitrary paths/messages/URLs/manuscript text are never accepted into records. Native child/renderer failures, path refusals, bounded canonical JSON syntax/read failures, and atomic-save failures are instrumented. Semantic validation remains with existing shared validators; native diagnostics do not parse arbitrarily large documents or collect their contents.
- A single per-window native recovery owner handles render-process-gone/unresponsive. The native dialog explicitly says the last save may not have completed and offers saved-project reopen, Home restart or Cancel. Root/generation changes invalidate stale answers; window disposal removes listeners; repeated incidents do not create duplicate dialogs. Restart replaces only the failed window after an explicit choice and preserves canonical/recovery files.
- `node scripts/verify-native-diagnostics.cjs`:PASS. Two thousand secret-bearing inputs remain bounded/redacted; storage ENOTDIR cannot damage its obstruction; registered file-handler path refusal, corrupt JSON and injected ENOSPC retain exact original bytes and remove owned temporary files; crash/unresponsive/retry/cancel/stale-root/disposal checks preserve one lifecycle owner. `node --check electron/main.cjs`:PASS.
- Actual Electron43.7.3 Linux x64 private-display crash evidence: `test-results/native-crash-2026-09-21T06-12-20.400Z`, PASS0.45s. `forcefullyCrashRenderer()` emitted reason crashed, one dialog decision reopened the saved project, and canonical/recovery SHA256 values remained exact. Persisted `artifacts/native-crash-evidence.json` was read and inspected: failed/reopened records share one operation ID, with no project path or draft content. The expected forced-crash stderr, DBus namespace warning and unavailable GLX notices are retained. The fixture uses the production recovery module with an injected native dialog answer; it does not claim physical desktop dialog interaction or non-Linux qualification.

All newly delegated implementation is complete. The final package/native/Paper/UI qualification remains root-coordinated. No release was published and no unavailable platform check is reported as passed.

## Closure audit and controlled follow-ups (2026-09-21 07:13 UTC)

The read-only final audit found two additional ownership windows and one verification metadata gap. They are now closed:

- SH01 flushes capture registration epochs and compare the current scoped owners after the awaited saves. A new/replaced/re-registered owner aborts Home/open/close instead of treating its buffer as covered by an earlier snapshot; unrelated pane ownership changes do not block a scoped close. Old disposers cannot remove a same-object newer registration. No input lock or artificial delay was added.
- SH03 live context now observes embedded root, tenancy and owner registration/readiness. Figure/Slide explicitly announce readiness after their asynchronous loads, so a context emitted during a long source catch-up cannot remain empty until the next unrelated input. Dispatch's existing resident-owner and persistence guards remain unchanged.
- `node --import tsx scripts/verify-shell-owner-epochs.ts`:PASS. Exercises actual goHome during a pending save with a newly registered dirty owner, exact retained draft/root/view, registration ABA, unrelated pane change, and production installBridge publication after delayed readiness/tenancy/root changes.
- VR01 explicit `FLUX_PRIVATE_DISPLAY=1` (or FLUX_XVFB) removes inherited WAYLAND_DISPLAY, selects X11 in the child environment, direct/nested Electron argv and disposable media launcher. Ordinary test display choice stays intact. `node scripts/verify-runner-runtime.mjs`:PASS; native/private and ordinary inherited-Wayland cases are covered. `npm run check:headless`:0 errors. The concurrent renderer check found four callback annotations in PS's in-progress export work; PS owns their correction and root owns final frozen checking.
- Browser driver, production startup and installed smoke now retain observed runtime records: viewport, screen size, DPR, CDP browser/GPU, CPU/RAM, source digest and served module URLs (installed smoke uses its actual bundled version result). Native packaged smoke connects with defaultViewport:null and asserts the actual native minimum window. An absent environment artifact is explicitly not an observation. The prior800×600 packaged screenshot was a synthetic Puppeteer viewport and is not final native-layout evidence.
- W4/W5 fixtures now preserve `?fixture=demo` when FLUX_URL redirects the server. The old expression discarded the fixture query and created a legitimate unsaved in-memory project with no disk model; saved-byte, failure/retry and lifecycle assertions were retained.

Controlled reruns, all under `test-results/runs/`: shell-fortificationPASS6.1s `2026-09-21T07-12-08-135Z-16`; W4PASS13.0s `07-12-14-600Z-320`; W5PASS12.8s `07-12-27-979Z-592`. These three sourceChanged=true cohorts overlapped other implementation and are diagnostic, not final source qualification. Their runtime artifacts were inspected:1440×900 viewport,800×600 headless screen, DPR1, ANGLE SwiftShader software renderer, exact source digest and Vite module URL. Direct Electron capturePASS0.3s `07-12-41-164Z-862` and PDF inert-scriptPASS1.2s `07-12-41-768Z-935` both sourceChanged=false; the prior Wayland startup timeouts remain preserved.

Earlier actual Linux diagnostic deb/AppImage evidence is at `release/v020-local-2026-09-21T06-32-23.117Z/package-evidence.json` and its README. Both extracted archives exercised13 installed behaviors including real CLI video pixels, native PTY, saved bytes, script-disabled PDF, resident worker, capture and local documentation navigation/search. Those artifacts precede these latest fixes and will be rebuilt for final evidence. They are explicitly non-distributable: the existing signed Firefox XPI payload is stale, and no signing/upload/publication was performed. Root owns final frozen cohorts, fresh package reruns and the overall evidence index.

### 2026-09-21 07:40 UTC — independent Figure acceptance audit followup

F01: a missing `fig/index.json` now requires a truthful, project-confined inventory before the shared snapshot can authorize a new subsystem. Existing canvases/assets/captions, index backups/quarantine copies, and manifest figure registrations make it incomplete; unavailable/denied inventory fails closed, including the explicit overwrite path. Empty scaffold directories and unrelated rendered files remain supported. GUI load/source/conversion/force and Node readers share this contract. Native `readdir(path, true)` propagates errors without changing the historical default directory-listing behavior.

Verification: `node --import tsx scripts/verify-figure-missing-index.ts` PASS13 in0.36s, with actual injected native file handlers, exact preserved authored bytes for GUI reload/save+Node compose+conversion refusal, native/Node/GUI EACCES, cross-approved-root symlink refusal, fresh GUI save and Node compose/reopen, and indexed legacy positive coverage. Existing `verify-v020-figure.ts` PASS0.31s, `verify-v020-conversion.ts` PASS17/0.32s, and `npm run check:headless` PASS2.4s after adapter integration. No physical-platform/native-window check is implied by injected native handlers.

F06: headless semantic export resolves both the asset-local and legacy in-project source manifest through the canonical project asset boundary. Only ENOENT is optional; unreadable/corrupt/escaping semantic metadata refuses output rather than silently discarding group overrides. Explicit external imports retain their copied asset-local manifest workflow. `node --import tsx scripts/verify-render-manifest-confinement.ts` PASS7/0.27s, inspecting actual exported group override markup, in-root symlink compatibility, zero external file reads through an escaping source link, real materialization failure with exact prior export bytes preserved, malformed sidecar refusal, and external-import compatibility.

The independent audit also identified the separate figure-reference journal's manuscript-lease/realpath/empty-evidence gaps; root owns their implementation and evidence. Earlier Linux archives predate these source changes and remain diagnostic only; fresh packaging and the current signed-Firefox blocker are tracked by root.

### 2026-09-21 09:16 UTC — gallery native fixture lifecycle correction

The frozen native cohort's gallery failure occurred because the fixture considered the newly mounted `.slide-mode` loading wrapper ready, then attempted to click an absent toolbar during “Opening slides…”. It now waits for the resident fixture element and enabled “Plots & videos” control in the active pane. Actual pointer input, native playback/capability release, keyboard focus, source/deck byte preservation, and the100ms preview threshold are unchanged. Artifact output now uses the runner's attempt-owned `FLUX_OUT`.

Focused runner verification **PASS** in1.7s at `test-results/runs/2026-09-21T09-15-56-426Z-2`, sourceChanged=false (dirty fixture edits over8b6223d). All11 native assertions pass; measured image preview32.5ms and pinned video35.0ms. Actual dissection and native-video PNGs were opened and inspected. The wrapper compared the saved deck and source SVG bytes exactly. First setup attempt `09-14-55-227Z-2` failed before Electron because `/usr/bin/Xvfb` is absent; retained evidence distinguishes that unavailable path from behavior. Successful invocation uses the existing isolated Xvfb executable `/tmp/flux-stash-xvfb/extracted/usr/bin/Xvfb`, disposable config/project, Mesa software rendering, and explicit test-only sandbox flag. No production change or physical-display qualification is implied.
