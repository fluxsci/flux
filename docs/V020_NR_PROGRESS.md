# V0.2 native / references implementation ledger

Worktree: `/tmp/flux-v020-fortification`, branch `codex/v020-fortification`.
Owner: native_references; shared lock contract owned by root, renderer flush owned by shell_verification.

## Implemented contracts (acceptance still in progress)

- NR-01: canonical text/JSON errors distinguish missing from corrupt/unreadable; annotation/organization mutation preserves canonical bytes on parse/read failure. Strict schema validation retains unknown fields. Initialization now holds shared library lease and publishes create-only. Tag add/remove express intent against fresh locked data. Assign/Zotero hold and release opaque native nonce.
- NR-02: shared raw-span BibTeX scanner distinguishes structural records, nested fields, escapes and quoted/concatenated values. Mutation planners reject malformed incoming/existing content; structural records survive import. Field, DOI, attachment and date edits share spans. Distinct authoritative DOIs cannot collapse by title signature. New keys check case/NFC and unsafe filesystem components. Existing unconventional item directory migration is not yet implemented.
- NR-03: sender/request-bound result coordinator; only saved closes automatically. Timeout/failure native retry/cancel/explicit discard; cleanup moved from before-quit to confirmed will-quit. Renderer ACK integrated by shell owner. Native gate adaptation still pending.
- NR-04: filesystem capability checks compare real filesystem identities, including nearest existing parent for creation. Legitimate symlinked project roots remain usable; symlink escape denied. Failed remove/trash fallback rejects; all atomic-temp failure branches clean up. Residual hostile local parent-replacement race remains (no portable openat). IPC payload/session audit and static print policy pending.
- NR-05: normalized IPv4-mapped IPv6 blocks private addresses; streamed metadata/PDF body caps; saved credentials only injected into explicitly approved HTTPS origins, rechecked inside page script. Login windows pin isolation/sandbox/no Node and deny popups. DNS transport pinning/proxy capture and broader metadata bounds/retry audit pending.
- NR-07/08: PDF presence no longer trusts parent-directory mtime; replacement invalidates stale fulltext; deferred linked extraction copies bytes before reader transfer. PDF task rejection enters finally and worker destruction runs even if task.destroy rejects. Reader supplement/snips/context and World queries use epochs/source snapshots. Further multi-file/reload/draft coverage pending.
- NR-09: removed fixed150ms local metadata query delay; normalization offset map includes original Unicode offsets. Resident fulltext service and incremental PDF match UI pending.
- NR-11: download setup covered by finally; app-owned progress tolerates dead sender; remove/shutdown cancel+await installation; startup handles spawn error and rechecks generation across readiness awaits. Additional injected race tests pending.
- NR-12: shared process runner delivers EOF, bounded tails, typed signal/error/timeout/cancel status, POSIX process groups and Windows taskkill escalation. GUI/CLI recipe runners use it; trust displays exact argv/cwd/env; true exit0 rereads emitted output contract, atomic metadata write, failed runs return no stale output. Native explicit cancellation UI, owner lock and emitted-malformation recovery pending.

## Exact verification so far

- `node --import tsx scripts/verify-canonical-reads.ts`: PASS11, corruption mutations inspect exact saved bytes.
- `node --import tsx scripts/verify-native-boundaries.ts`: PASS14, real file-core handlers with disposable symlink fixtures, streamed cap and sender/timeout coordinator.
- `node --import tsx scripts/verify-process-runner.ts`: PASS7, real child EOF/flood/signal/timeout/abort/missing executable.
- `node --import tsx scripts/verify-bib-scanner.ts`: PASS11, nested/pseudoentry/DOI/case/stamp/malformed source.
- Existing `verify-correction-runtime.ts`: PASS40; `verify-organize.ts`: PASS35; `verify-p5-library.ts`: PASS source checks; `verify-w20-library.ts`: PASS; `verify-zotero-sync.ts`: PASS38 including real CLI and saved PDF bytes (isolated HOME/XDG).
- Initial scanner test contained missing closing brace in its synthetic DOI fixture; strict scanner correctly refused it. Fixture repaired, assertions retained.
- Initial `verify-an-journal-locks.ts` invocation exposed its scaffold machine-root initialization and failed before publication (missing lock directory). Subsequent machine-sensitive gates always use disposable HOME/XDG. Root fixed shared lease lock-dir creation.
- `npm run check`: last run2 unsupported PDF.js-option errors and root-owned Figure pane warning; correction underway. PDF.js6 removed `isEvalSupported`; do not claim an ignored option is security enforcement.

## Qualification

No production/user-data writes, paid services, downloads, publication, installed-browser extension, Windows/macOS native or physical display checks. Gate results above are component checks, not completion of any entire NR package. Full package acceptance/required UI/native suites remain open until recorded below.

## 2026-09-21 implementation wave 2 (ongoing)

- NR-01: global watcher classifies organization and suppresses only the originating renderer; canonical annotation models retain read snapshots and recheck immediately before save. Organization/library writers also compare read bytes; destructive GUI BibTeX removal preserves `.bak`. Native keys use the shared operation lease, strict canonical reads and restrictive temp permissions from creation. Noncooperating external writes retain a small check-to-rename race; local leases cannot arbitrate independent synced machines.
- NR-02: macros/concatenations and common TeX accents project through the shared scanner, `bibtexSummary` uses it, raw comments/trivia keep their order on import. Restore rejects intervening identity conflicts and preserves original dateadded. **Remaining:** versioned unsafe legacy item locator/ambiguity detection and original-order undo; no silent migration attempted.
- NR-04: live main-frame session/document validation wraps every IPC, structural caps reject cycles/nonfinite payloads. `projectAssetPath` confines saved asset paths to exact current/pending project canonical roots (or exact chooser directory), independent of external read grants. Static print now has an isolated in-memory session, deny permissions/downloads/popups, no network egress, script-disabled rendering and job-specific file roots. Real native exports still require final validation.
- NR-05: Node public transport supplies DNS validation as the connection's resolver, preserving original TLS host/SNI and checking redirects; fixed metadata/PDF clients use streamed mode caps. Chromium publisher transport deliberately retains its separate preflight-DNS residual risk (per plan permitted exception; no publisher heuristics/timing rewrite). Proxy in-page capture streams with caps; CDP/download paths cap declared/observed bytes. Metadata positions/chunks are bounded, keys reject corrupt stores, total hydration failure reports failure with old cache retained. Remembered origin approval is exact HTTPS and rechecked inside page script. POSIX0600 is not Windows ACL qualification.
- NR-06: intake/discard/park serialized, source-qualified nested names, ambiguous legacy names refused; exclusive hardlink publication and EXDEV-only copy/fsync fallback. Extension deadline aborts requests/downloads and no longer reports timeout as success. Archive inflation caps2000 members/80MiB member/256MiB total; snips cap64MP/16384px without changing requested DPI. **Remaining:** installed Chrome/Firefox service-worker restart/cancel qualification and complete cross-device checksum fault acceptance.
- NR-07: PDF/provenance publication holds per-item operation leases and writes recoverable `source.pending.json`; text invalidation precedes replacement, annotations preserved. Fulltext metadata records source generation; GUI/Node extraction compares generation before commit; Node compares bytes for passed extraction identity. Search/index refuse stale/incomplete generations and linked sources changed/missing. Supplement publication serializes names and manifest merge, preserves distinct concurrent bytes. Snip publication is project-lease-owned with sidecar reservation and Node exclusive PNG publication. Zotero settings are in sync fingerprint, failed attachment sweep stays retryable; GUI iterates all candidates, Node reports remaining attachments. GROBID rejects reprojecting old TEI onto changed PDF identity. **Remaining:** generation recovery UX, unsafe legacy item paths and large-library native acceptance.
- NR-08: awaitable retained annotation drafts join shell flush, origin/reload epochs avoid cross-pane rollback; source snapshots govern supplements/snips; focused context writes serialize. ReaderLibraryPanel reloads and errors have epochs; resize cleanup restores prior user-select; refine-scroll retries cancel on source/target lifecycle. Failed draft/error presentation and real two-window/native close still need final acceptance.
- NR-09: resident worker per canonical library root (bounded queue/deadline, superseded-owner cancellation, restart/dispose), incremental PDF.js match descriptor adapter with lazy snippets and >200-result virtualization. Both worker build and unpacked packaging entries added. Original Unicode offsets corrected. **Remaining:** final measured300/1000/5000-paper native corpus/heap qualification and source-frozen scale suite.
- NR-11: injected setup/spawn/remove/prime shutdown failures now covered by extended runtime gate; resumed range header validated (old fixture's incomplete206 response repaired with correct Content-Range).
- NR-12: GUI/CLI share process runner and per-recipe operation lease; exact argv/cwd/env trust snapshot, bounded output and generous24h deadline. GUI exposes sender-owned explicit cancel. Previous good recipe saved before execution and retained on command/malformed-emitted failure; successful emitted contract reread under lease. **Remaining:** native cancel/descendant and malformed-emission gate extension, Windows process-tree qualification.
- NR-10 initial wave status: extraction waited for behavioral acceptance. The later native extraction closure below supersedes this initial native-family deferral; no proxy heuristic restructure.

### Recorded verification

- `verify-canonical-reads`: PASS11; `verify-native-boundaries`: PASS21 (actual file/contract/coordinator handlers, connection-resolver injection, streamed caps, metadata bombs); `verify-process-runner`: PASS7; `verify-bib-scanner`: PASS11 before added macro/undo cases (new cases await rerun).
- `verify-item-generations`: PASS15 with disposable saved-byte inspection: stale A/B extraction denied, external replacement excluded from index/search, broken link availability, exact annotation preservation, concurrent supplements + manifest, incomplete provenance marker.
- `verify-correction-runtime`: PASS47 including ENOTDIR then retry, dead sender progress, emitted spawn ENOENT, remove during streaming install, shutdown during prime. Resume fixture amendment awaits rerun.
- `verify-fulltext-service`: PASS7; `verify-reader-ownership`: PASS13 (including91,500 exact matches/300pages; incremental component643ms cumulative vs reviewed15.7s; not an OS/native ≤100ms qualification); `verify-capture-integrity`: PASS5.
- `verify-ipc-contract`: PASS129 channels before recipe cancel/new concurrent channel additions. `verify-snips`: PASS49. `verify-f2-recipe` (isolated HOME/XDG) and `verify-fluxplot-recipe-ipc` passed real generating process, exact emitted bytes/provenance. `verify-electron-hardening`: PASS13 source-shape checks, not native assurance.
- `npm run check`: latest completed0 errors/0 warnings before latest recipe/Reader edits. Headless check found one missing generation local in assign; repaired; rerun pending.
- Reader group first meaningful run: **12/17 pass**, `test-results/runs/2026-09-21T04-22-33-830Z-2`. r1/r7/fulltext/snips cite/scale require rerun after coordinated source freeze; explicit execution-context-destroyed and source changes observed. Earlier group blocked by runner's overlong TMPDIR Unix socket (shell owner fixed), not attributed to production.
- Two direct scaffold-based test invocations omitted isolated HOME and failed at real FluxLib arbitration ENOENT before publication; all further machine-sensitive runs must go through isolated runner or explicit HOME/XDG. Do not represent these as successful hermetic runs. Parent owns final preservation inspection.

## 2026-09-21 implementation wave 3 — scoped source freeze and native evidence

This entry supersedes the implementation/verification gaps in the earlier wave entries where explicitly closed below. Root owns final broad runner results, guide updates, release checks and cross-package acceptance. No release was published. All native fixtures below used isolated HOME/XDG paths before invoking the Electron wrapper and the task-owned Xvfb display :187 (Mesa software rendering, no GLX), never the running user's display/project. Electron 43.7.3 / PDF.js 6.3.289 were the installed versions tested. One earlier wrapper invocation without an isolated cache attempted dependency setup and failed with ENOENT before installation; parent was notified, subsequent dependency setup used `/tmp/flux-nr-native-cache`.

### Completed changes since wave 2

- NR-01/F02: canonical mutations refuse existing Syncthing conflict copies using the established shared conflict rules. GUI and Node additive tag intents merge under the same operation lease. Global watcher includes organization and suppresses only the matching originating file generation; an external rewrite during the former 1500ms self-write interval cannot inherit a renderer's identity. Explicit lease assertions now precede every owned canonical/PDF/link/text/supplement/snip publication and invalidation step. Public annotation saves acquire a lease if a containing transaction did not supply one.
- NR-02: the shared reversible versioned item locator preserves conventional folders, reads uniquely attributable unsafe legacy folders, maps directory scans back to original citekeys, and refuses ambiguous case/NFC/sanitized aliases without migration or overwriting. Bib deletion Undo preserves exact original bytes when unchanged, original relative order plus intervening edits/additions otherwise, and refuses intervening same-key identity conflicts.
- NR-03/08: actual main/preload/root-flush integration now exercised with a retained note draft and an actual failing native write. Native Cancel preserves both windows and unsaved text; continued editing and a delayed successful retry publish exact bytes before only the requesting window closes. Existing quote-based detached-highlight UI remains in place. Citer/presence caches include root and generation; stale loads cannot repopulate a newer source. Fulltext query clear/unmount invalidates the latest result owner and cancels the native request.
- NR-05: only404/410 are definitive absence; authentication/environment/transport errors remain retryable and do not poison durable miss state. Retry-After supports seconds/dates with bounded30s per-origin cooldown. Proxy origins require HTTPS without embedded credentials. Native request cancellation is sender-qualified, and renderer destruction cancels only owned capture jobs. Fixed metadata/Node public requests use the shared vetted connection resolver; Chromium publisher heuristics remain unchanged under the plan's explicit transport exception.
- NR-06: opaque capture IDs identify the observed file generation and single active consumer. Stale IDs cannot dispose replacements; exact completed IDs are idempotent. Parent symlink changes after intake are rejected. Shared `verifiedMove.cjs` publishes complete bytes exclusively, uses EXDEV-only copy/fsync/hash fallback, preserves originals on copy/unlink failure and resumes a recorded destination without duplicate suffixes. GUI/Node Assign use the same verified movement primitive and do not discard input after rejected attachment. ZIP/sidecar/stream/raster bounds are retained.
- NR-07: automated attachment now rechecks current PDF inside the item lease; a late OA/proxy result cannot overwrite a manual/current attachment. Explicit refresh still authorizes replacement. Fulltext is invalidated before PDF/provenance replacement and only a matching source generation may publish extraction. A revoked writer preserves previous PDF bytes and leaves an explicit recoverable pending marker. Supplement retry repairs missing manifest metadata for already published identical bytes; manual GUI supplement ingest uses the same reserved publication/manifest transaction. Zotero failed attachment passes remain retryable and changed attachment policy invalidates its fingerprint; stale GROBID TEI cannot be restamped as current PDF content.
- NR-09: bounded resident worker, exact scan oracle and Unicode source offsets preserved. Parallel bounded freshness probes and ASCII-run folding removed avoidable repeated filesystem/normalization cost. No database, silent hit cap, threshold relaxation or index representation replacement. The production UI's explicit result limit remains distinct from complete all-hit corpus measurements.
- NR-10: targeted tested responsibilities have owners (`appLifecycle`, file capability core, operation/gui leases, resident fulltext service/worker, process runner/recipe lease, verified move and item locator). The remaining specified native families are extracted below. Library view splitting is conditional on query/mutation controllers, as detailed below. No publisher-capture heuristic refactor or cross-app Lighttable imports.
- NR-11: correction-runtime lifecycle47-case gate retains its failure-injection coverage; shell owner also verified existing Linux correction release inventory (read-only, no real model download).
- NR-12: bounded process runner distinguishes output-sink failure, spawn/signal/cancel/deadline outcomes, closes stdin, bounds tails, and reaps descendants. Actual native requesting-window cancel refuses a foreign renderer, returns no stale prior SVG and stops its child/grandchild. Trust dialog displays the actual argument boundaries/cwd/environment/deadline. Malformed emitted recipe keeps the prior-good snapshot and failed output remains distinguishable.

### Exact verification and artifacts

| Gate / artifact | Latest recorded evidence |
|---|---|
| `verify-canonical-reads.ts` | PASS15; real lease revoked after canonical read, exact prior organization bytes retained; malformed/schema/conflict copies fail closed. |
| `verify-native-boundaries.ts` | PASS29; real handlers and filesystem symlinks, generation-aware write origins, sender/payload caps, mapped addresses/connection resolver, streamed caps, secret temp0600+failure cleanup, blocked/foreign/timeout ACK outcomes. |
| `verify-bib-scanner.ts` | PASS18; lossless spans/macros/accents, DOI identity, exact/order-preserving Undo and conflict refusal. |
| `verify-item-locators.ts` | PASS11; actual unsafe legacy PDF bytes/search identity, ambiguity preserves bytes/directories, UNC prefix fixture. |
| `verify-item-generations.ts` | PASS27; manual attachment preserved against late automation/Assign/additive Zotero, explicit refresh, actual revoked item lease, prior PDF bytes/recovery marker, stale extraction/index exclusion, broken link and supplement publication/manifest. |
| `verify-capture-integrity.ts` | PASS19; opaque claim/idempotence/stale replacement, exact nested source, post-intake parent symlink swap, EXDEV corruption/unlink failure/retry exact hashes and receipt cleanup. |
| `verify-process-runner.ts` | PASS9; actual stdin EOF/output flood/spawn/cancel/timeout/resistant descendant/output-sink failure. |
| `verify-reader-ownership.ts` | PASS18; failed retained draft flush/retry, Unicode original UTF-16 offsets, exact91,500 matches and incremental300-page collector. Component CPU gate is not native display qualification. |
| `verify-fulltext-service.ts` / `verify-fulltext-search.ts` | PASS7/PASS22; worker reuse/cancel/restart/root ownership and exact search semantics. |
| `verify-scale-fulltext.mjs` | Existing unchanged thresholds PASS; first build551ms, warm125ms (<300), one-document update171ms (<300); complete existing correctness assertions retained. |
| `verify-fulltext-corpus.ts` | PASS exact18 query/oracle comparisons over300/1000/5000 papers. Artifacts `test-results/fulltext-corpus/fulltext-corpus.json` and `test-results/fulltext-corpus-after/fulltext-corpus.json`. |
| `verify-pdf-inert-electron.cjs` | Actual native PASS on :187; `test-results/pdf-inert-probe/pdf-inert.json` and inspected `pdf-inert.png`: PDF.js worker paints scientific text and INERT AcroForm while embedded OpenAction/JavaScript remains inert. |
| `verify-library-native.cjs` | Actual native PASS on :187, `test-results/library-native-probe/library-native.json`: concurrent2 GUI+Node tags, both-window external notification, foreign lease release refused, real revoked lease prevents publication, exact corrupt bytes, native recipe owner cancel/tree stop, native failed annotation write→Cancel→continued edit→delayed save/close exact bytes. Dialog choice automated; shipped main/preload/lifecycle/draft/process logic exercised. Expected EISDIR and Lost lease logs are injected failures. |
| Existing related suites | GROBID50, Zotero41, assign-hardening27, snips49, correction-runtime47, electron-hardening13, native recipe IPC, supplement and repaired fluxplot03 PASS. Source-shape checks are labeled structural and are not substituted for behavior. |
| Type checks | `npm run check:headless` exit0; `npm run check`0 errors/0 warnings after prepublication guards and current source changes. |

Corpus reference hardware: AMD Ryzen Threadripper PRO9985WX64-core /128 logical cores,535,121,620,992 total RAM bytes, Linux Node22.17.0. Each document12pages×160tokens with realistic shared scientific vocabulary and50k deterministic transcript identifiers. At5000papers:97,203,662 source-text bytes,55,063 vocabulary terms,5,000,000 page postings,35,998,739 index bytes. Before optimization cold12,157.66ms, warmP508081.94ms/P958339.59ms; after cold4219.96ms, warmP501658.29ms/P951918.76ms, max main timer gap12.08ms, one worker process. At300 warmP50/P95128.73/134.84ms;1000 warm354.17/399.04ms. The5000 all-hit query still exceeds100ms completion; the worker keeps main responsive and UI cancellation available. Memory/heap figures in artifact include explicitly identified worker/main oracle allocations; do not represent them as browser heap or an owner-display latency measurement.

### Remaining acceptance and explicit limits

- Root must record source-frozen `reader-gate`, Paper gates touched by shared flush/contracts, and broader pure/build/package acceptance;20k metadata/direct-interaction measurements are now recorded below; earlier Reader12/17 run is superseded only by an explicit successful final run, never by these focused gates.
- The earlier marker-retention recovery limitation is closed below by deterministic read-time recovery. Unrecognized/conflicting PDF bytes or corrupt recovery metadata remain preserved with an actionable reattach error; the application cannot safely infer an unknown source. Legacy annotations retain their exact original bytes and existing detached-highlight presentation; an original PDF hash cannot be reconstructed retrospectively.
- Canonical byte comparison and lease ownership checks protect cooperating writers; native check-to-use filesystem races against hostile/noncooperating external processes and independently synced machines remain outside local lease arbitration. Stored-asset symlinks and observed capture-parent swaps are nevertheless actively rejected.
- Chromium authenticated publisher transport retains the documented preflight-DNS connection-binding residual risk; fixed public Node/native metadata requests do bind vetted DNS to connection. Institutional SSO/cookie/publisher compatibility was not exercised live.
- No Windows/macOS runtime, secure-storage/ACL, native menu/close, signed package, installed Chrome/Firefox extension worker-restart/capture, Wayland/NVIDIA physical-display or institutional authentication qualification. Linux simulated UNC/menu checks are not target-platform passes. No paid/live provider/model service or release publication.


### 2026-09-21 NR07 recovery closure

Plan03 lines119/125 require recoverable publication plus intact scientific bytes/no old-source context; they do not expressly authorize treating indefinite marker retention as completed recovery. The shared `itemRecovery.ts` now reconciles pending items during normal source/fulltext/Reader reads under the per-item lease. Exact current-PDF SHA matching the pending source completes provenance; exact match to existing prior provenance rolls back only the pending marker. PDF and annotation bytes are never rewritten by recovery. Both paths invalidate ambiguous derived text, compare marker/content again and check operation ownership before completion. Unknown or malformed identities preserve every canonical byte and report the precise reattach/retry action. Normal items pay only the existing pending-file presence probe; hashing is limited to interrupted items.

Linked publication now stages the same pending record with exact serialized proposed/prior pointers. Recovery can finish/roll back the pointer metadata without opening the external PDF, preserving deferred Zotero semantics. A conflicting stored PDF or unrecognized pointer remains blocked and intact.

`verify-item-recovery.ts`: PASS23. Covers real child process exit86 immediately after actual writer's PDF rename, ordinary read completion, rollback before replacement, exact prior/new PDF/source/annotation bytes, deferred link completion/rollback with missing external targets, EACCES during recovery then retry, idempotence, and malformed/unknown generation preservation. `npm run check:headless` exit0 and `npm run check`0errors/0warnings after recovery implementation. Root registers shared recovery/twin path mappings. Extended native gate adds actual preload-driven GUI read recovery; result recorded after its scheduled run.


### Final focused results before broad root gates

- `verify-library-20k.mjs`: PASS. Ten free-text/structured/clear queries against20,000 real loaded metadata entries checked exact logical totals and visible row identities. Every visible response≤100ms; p5030.0ms, p9531.3ms, max frame gap16.7ms, no task>100ms, bounded≤60 rendered rows and zero console errors. Hardware/browser/viewport and every sample are in `test-results/library-20k/library-20k.json`; `library-20k.png` was inspected and shows20,000 references with the real Library layout. This headless Chrome result is not physical-display qualification. The older5000-entry gate was unchanged.
- Final `verify-library-native.cjs`: PASS with the actual GUI recovery adapter via preload. A pending generation with exact matching PDF completes source metadata, removes stale preceding text and pending marker, and preserves exact PDF/annotation bytes. The same run also passes real lease revocation before publication,2 GUI+Node writers, cross-window notifications, recipe process-family cancellation and failed native note-save close/retry. `test-results/library-native-probe/library-native.json` includes recovered provenance and `leaseRevocationPreserved:true`.
- Generation identities now include native filesystem change time in addition to size/mtime; exact same-size PDF replacement with restored mtime cannot keep old text valid. Fulltext index freshness likewise includes change time so same-size/restored-mtime extracted-text replacement rebuilds candidate postings. `verify-item-generations.ts` PASS27. This adds an optional native stat field and only invalidates derived receipts; canonical/scientific bytes are unchanged. Corpus representation measurements above predate this small extra per-document index field, as recorded rather than silently recomputed.
- `npm run check:headless`: exit0 after the final change-time field; renderer check0errors/0warnings after all GUI/source-recovery changes. Zotero41 and assign-hardening27 were rerun after the inside-lease additive-attach guard and passed. Source is frozen for root's final broad UI/build/package gates. No pending scoped native/browser process remains; the task-owned Xvfb display :187 is retained for root's packaged checks.


### 2026-09-21 NR10 native extraction closure

The behavior gates made the native prerequisites in plan03 lines158–164 hold. Main now composes independently instantiated `ipc/capture.cjs`, `globalLibraryWatcher.cjs`, `ipc/readJobs.cjs`, and `ipc/staticPrint.cjs`, in addition to already extracted leases. Changing library/capture/config/resource values are explicit getter dependencies. Capture owns intake concurrency/onboarding; the global watcher owns root rebuild serialization and pending notifications; read jobs own the resident fulltext service; static print owns its queue/window/private session. No service locator, cross-family mutable singleton, publisher heuristic change, or renderer markup/CSS change was introduced by this extraction. Diagnostic/crash recovery belongs to SH's separately tested native modules.

Native lifecycle changes are limited to terminal disposal and setup-failure cleanup: no watcher revival during awaited setup, no duplicate handler/policy registration on recreation, pending watcher notifications canceled on close, print temporary directories removed even if window creation fails, and print requests cannot publish after disposal. A strict real PDF artifact assertion discovered an existing blank second page caused by a full-size inline SVG baseline. The figure print template now makes its top-level SVG a block; exact page-size and one-page assertions remain mandatory.

`verify-native-families.cjs` PASS28, without Electron: current-root getters, capture single consumer/disposal, watcher setup failure/retry/root change/queued notification origin/async disposal, read worker reuse/root disposal, separate static-print sessions, serial exact content, blank/recreate/no-script/network refusal, failed setup temporary cleanup/retry, hung asset readiness deadline, missing asset refusal with exact prior output, terminal publication refusal. Its synthetic PDF bytes test ownership only; the real artifact gate is separate. `verify-ipc-contract.ts` PASS133 unchanged public channels; `verify-electron-no-undef.ts` PASS2 over44 native modules; `verify-electron-hardening.ts` PASS13; capture-intake assertions all pass after source-shape references were updated to the registered extracted modules. W12 PASS after its fixture was corrected to create disposable HOME/config/cache before engine import. An earlier mistaken direct invocation without HOME isolation failed ENOENT at the real FluxLib lease arbitration path; the parent preservation audit was notified and no real-data result is claimed from that failed invocation.

Resident worker lifecycle acceptance is strengthened by `verify-fulltext-service.ts` PASS12: real index reuse/cancel and changed-child exact results, controlled synchronous startup failure followed by retry, real worker runtime crash followed by successful restart, and a blocked synchronous worker terminated at deadline. Synchronous constructor/postMessage failures now settle the queued request and cannot strand the service.

Conditional view extraction: plan03 line160 requires stable query/mutation controllers before Library grid/facets/details/world/settings splitting. The tested pure query runner and canonical mutation bridges are stable, but Library's World paging epoch, result-selection/bulk-action state and settings draft state remain component-owned; no unified view-controller API for those operations exists yet. Reader's failed annotation persistence is extracted into `annotationDrafts.ts`; source epochs, context publication and snip snapshot checks remain integrated with the existing PdfView handle and three-document LRU. The current acceptance protects those ownership boundaries and demonstrates no need for markup/CSS movement. A future broad view split must first expose/test those controllers with deferred World paging, two-pane source switching and in-flight snip/context publication; parent owns this explicitly conditional P2 decision. Native-family extraction is not deferred on that basis.


### 2026-09-21T06:15Z final NR source freeze

Final actual `verify-library-native.cjs` PASS after every NR production change. `test-results/library-native-probe/library-native.json` records both real windows plus Node, exact revoked-lease and corrupt-byte preservation, ordinary GUI interrupted-PDF recovery, requesting-window recipe cancellation/descendant termination, and failed note-save close/Cancel/continued-edit/retry. Extracted capture count/onboarding and repeated fulltext search handlers ran through actual preload/main. Concurrent figure/document PDFs contain the exact expected scientific labels and numerical values42.5/23.75, and the figure has exactly1 page sized240×120pt. Saved `%PDF-` bytes, `pdftotext`, `pdfinfo`, and the rendered `native-figure.png`/`native-document.png` were inspected; artifacts remain beside the JSON. The document's embedded script remains inert. Invalid dimensions and an unprepared remote image both preserve the exact previous PDF. The latter produces a useful error without its credential query, and the isolated local HTTP server receives zero requests.

NR04 static print now bounds asset loading, printing and worker reset with a120s stage deadline, destroys a stalled worker, and rejects missing/blocked images/fonts/stylesheets before output publication. Chromium request failures and asset CSP failures become generic actionable errors without recording URL credentials/query strings. The pure deadline fixture stalls its window intentionally, proves destruction and exact old-byte retention, and does not claim an actual120s native timeout was measured.

Source is frozen; root owns the final integrated build, pathMap/full cohorts, Paper suite, packaged application checks and preservation inventory. No NR-owned browser/native child remains running. The existing task-owned Xvfb :187 remains available for root's isolated native/package checks.

| Package | Scoped implementation closure | Remaining qualification or conditional work |
|---|---|---|
| NR01 | Canonical missing/error/corrupt distinction, owner-held read/modify/publish, exact-byte guards, cross-window organization notifications. | Noncooperating external filesystem check/use race and independent sync hosts are outside local lease arbitration. |
| NR02 | Reversible item identity, unique legacy fallback, ambiguous/colliding paths fail closed, both engines use locators. | No automatic migration of ambiguous legacy identities. |
| NR03 | Correlated sender/request flush result; blocked write preserves window/draft and Cancel; successful retry closes only owner. | Physical Windows/macOS close/menu behavior unqualified. |
| NR04 | Canonical filesystem confinement, sender/payload/session contracts, atomic failure cleanup, static print flags/assets/deadline/refusal and bytes. | Portable hostile-parent replacement race remains; platform ACL/secure storage checks unavailable. |
| NR05 | Vetted connection-bound public fetch, redirect credential stripping, mapped-IP/size/deadline/retry outcomes, scoped key/proxy handling. | Live institutional SSO/publishers unavailable; Chromium authenticated transport retains documented preflight DNS binding residual. |
| NR06 | Opaque capture ownership, exact nested source disposal, verified EXDEV move/retry, receipt/idempotence; late audit closed actual extension state/restart/byte-cap and ZIP parser gaps below. | Installed Chrome/Firefox extension worker-restart/capture not qualified; heuristic restructuring excluded. |
| NR07 | Deferred linked bytes, generation-bound PDF/text/cache state, additive/manual preservation, supplement transaction, deterministic interrupted read recovery in both engines. | Unknown/corrupt pending identity deliberately preserves all bytes and requires user reattach/retry; prior missing annotation hash cannot be reconstructed. |
| NR08 | Reader source/session controller, native sender/root/nonce context publisher, PDF task owner, snip controller, retained annotation drafts/retry, finite restored layout bounds and focus cleanup; late audit tests below. | Root final rebuilt Reader cohort is still required after the late audit changes. |
| NR09 | Immediate prepared metadata queries; incremental exact PDF match descriptors/windowing; resident fulltext worker/cancel/restart/deadline; Unicode original offsets; measured corpus and20k acceptance. |5000-paper all-hit completion remains1.66/1.92s p50/p95 outside main; main gap12.08ms and20k direct responses≤31.3ms. Results are stated reference hardware/headless measurements, not all-platform latency guarantees. |
| NR10 | All specified native families independently instantiated/tested; SH owns diagnostics/recovery. Reader source/session, annotation persistence, context publisher and snip operation controller are extracted behind the existing PdfView handle. | Broad Library view splitting still requires stable query/mutation controllers for World, selection/bulk actions and settings drafts; the exact remaining conditional prerequisite is described below. |
| NR11 | Correction runtime setup/spawn/stop/remove/download state cleanup, signed-range/hash checks, stale generation rejection and bounded shutdown. | Real model download, paid provider and target-platform install were not performed. |
| NR12 | Shared validated bounded process runner, exact invocation trust, reproducible recipe snapshot/output handling, sender-bound cancel, EOF/status/output cap/tree termination. | Windows taskkill behavior unqualified on this Linux host. |

### Final independent integration review — recovery confinement/startup closure

Read-only review reproduced a cross-project overwrite through the legitimate native filesystem root union: a project A recovery journal targeting `fig/owned.json` could follow A/fig→B and replace B's exact matched bytes, then remove A's journal. The new shared `generationBridgeIO` applies project-specific realpath checks to the journal, all targets, and nearest existing parents for absent targets. Figure, Slide, conversion and source publication/recovery use that adapter. Shared text-generation recovery preflights the entire journal before changing any valid sibling; Node adapters now expose the same all-path preflight. Canonical project aliases and symlinks staying inside the project remain supported. Pure memory/browser fixture bridges have no host filesystem; the shipped native preload supplies the canonical path capability.

A second reproduced defect returned project.json's interrupted deck registrations from `loadProject` before Figure/Slide later rolled the generation back. The actual GUI entry now restores text/binary generations under ordered project→slides→manifest leases, after export recovery, before parsing/returning the manifest. Export recovery also validates generated temporary-resource paths before any source restoration. Root separately fixed the discarded Slide lease handle in live Figure source publication and added lease-loss tests.

`verify-generation-confinement.ts`: PASS14. Actual native file handlers and disposable real symlinks cover both engines' text/binary recovery, existing/absent outside targets, exact other-project and valid-sibling preservation, exact retained journal bytes, no journal on refused new publication, journal symlink escape, legitimate root alias/internal symlink/nested publication, generated export-resource preflight, and actual GUI loadProject restoration/refusal before manifest adoption. `verify-v020-conversion.ts` PASS17; `verify-v020-crash-recovery.ts` PASS all8 real SIGKILL barriers; `verify-v020-figure.ts` PASS after adapter changes. Renderer/headless checks passed after initial confinement implementation; root's final integrated checks cover the final startup and export preflight additions. No performance threshold changed and no performance tests were run during this review.

SH's package inspection found the bundled signed Firefox XPI predates the hardened background.js payload. Current Firefox extension qualification is therefore unavailable pending a matching signed artifact; an old signature is not evidence for the new payload. No signing or publication was performed. Final source/comment/ledger writes complete; further cohort evidence and packaging disposition belong to root.


### 2026-09-21T07:48Z late acceptance audit and final owned freeze

The final plan/COVERAGE audit found that earlier NR06/NR08 closure wording overstated extension restart/observed-byte handling and deterministic Reader task/context coverage. Those were implementation and test gaps, not unavailable-platform exclusions. This entry supersedes those claims. NR09 dirty-candidate/progress/integrity followups are root-owned and recorded separately; earlier corpus results are not evidence for those later changes.

**NR06.** The actual extension background worker now reconciles existing downloads before accepting a capture, retries transient reconciliation failure on the next action, and cancels only records bearing its exact extension ID and capture filename. Unattributable downloads remain untouched and report incomplete ownership. Early complete events and lost events are resolved against browser download state. Actual received/final bytes enforce64MiB per file and256MiB per capture; a90s overall abort invalidates late callbacks, and pending downloads at60s are queried/canceled rather than counted complete. Badge cleanup belongs to its capture generation. Existing permissions, filename convention, main-PDF-before-supplements, metadata-only fallback and publisher heuristics are preserved. No blob URLs are created. The bounded ZIP parser checks all central/local headers before retaining expanded buffers, supports STORED/DEFLATE, and refuses encryption, unsupported methods, invalid lengths/CRC, overlapping/truncated data, excessive counts and absolute expanded-byte limits. A proposed compression-ratio cap was removed because valid scientific CSV can compress extremely well; a real high-ratio fixture verifies compatibility. EPMC parser failure is actionable rather than an empty-success result.

`verify-capture-downloads.ts`: PASS27 against the actual background.js in a hermetic browser-API VM, with controlled callback order, worker restart, retries, clocks and byte observations; ZIP fixtures inspect exact ordinary/high-ratio output and reject malformed/inflation cases. This is deterministic worker logic validation, not installed-browser qualification. Existing extension and supplement gates also pass. `verify-assign.mjs` now builds a valid two-page scientific PDF in memory instead of depending on personal FluxLib papers; its9 browser checks retain DOI/title/page/text assertions and add exact metadata checks. Artifact: `test-results/assign-hermetic-late/assign-signals.png`.

**NR08 and NR10.** `taskOwner.ts` gives each PDF task an independent input copy and owns port/wrapper/task across setup failure, load rejection and teardown errors; Reader, signals and extraction use it. Installed PDF.js6.3.289 removed the legacy `isEvalSupported` option and PDF-data eval compiler. The false option is only a compatibility pin: it must not be cited as enforcement. A labeled structural guard checks the installed paired legacy builds for eval/Function compilation paths other than the exact core-js fixed `Function('return this')()` global-object fallback (unreachable with modern globalThis); the real native JavaScript/AcroForm fixture is independent behavioral evidence. PDFViewer receives no scripting manager, which is the installed v6 scripting enablement boundary. Invalid/password/zero-byte loading cleanup is exercised in both extraction paths.

Reader source/session and context publication are extracted into tested instance controllers; initial load and library reload share one root/epoch owner. Linked PDF identity observes actual source filesystem generation. A main click invalidates a pending supplement; current source changes invalidate snips. `readerSnips.ts` mechanically owns the existing render/publication operation behind the unchanged PdfView public methods. Captured source, citekey, citation, rect, root and view handle are immutable through awaits; project/source/disposal/lease changes refuse publication, and a late completed save cannot clear a newly created naming draft. The UI and raster DPI remain unchanged. Annotation drafts remain in the previously tested retained persistence controller.

The native Reader context family owns a canonical library root, sender, nonce and monotonic generation. Ownership is rechecked immediately before atomic rename; caller-supplied paths are removed. A new focused claim clears the previous document before publication. Pane teardown clears its matching claim; destroyed-sender clearing is best effort if the filesystem capability has already expired, with30s expiry covering crashes/refusal. A10s heartbeat keeps a live long read valid. Switching OS focus to an external agent retains the exact last committed selection with `foreground:false` and unchanged content timestamp; renewal cannot introduce background text. A newly focused Flux window supersedes that owner, and stale publication/clear is refused. Configuration moves suspend new claims and drain queued writes before directory rename, then resume with fresh claims; overlapping suspension is counted. Node context resolves the actual linked main PDF without materializing deferred bytes, prefers a stored copy, and names supplements without exposing main-paper fulltext.

`verify-reader-session.ts`: PASS63. Actual controller/file-handler tests cover failed setup/destruction, copied transfers, slowA/fastB/main/root/disposal ownership, saved PNG/sidecar equality and288dpi, delayed snip cancellation, pre-rename focus/root changes with exact prior-byte preservation, linked-source paths, foreign/stale context clears, external-agent blur/heartbeat, crash expiry, config-move drain through an actual directory rename, and cross-library context symlink refusal. `verify-reader-ownership.ts` PASS18 remains the annotation/Unicode/incremental-match gate. `verify-library-world-ownership.mjs`: PASS6 using real UI handlers and controlled provider promises, including old-query/page completion, failed page2 retry as page2 and clearing its old error after success; no live provider or unhandled errors. Artifact: `test-results/library-world-owned-final/library-world-ownership.png`.

Actual native Linux checks used isolated HOME/config/cache before the wrapper, task-owned Xvfb:187, Electron43.7.3 and PDF.js6.3.289. `verify-pdf-inert-electron.cjs` PASS: scientific text paints, AcroForm remainsINERT and OpenAction JavaScript does not run;12 repeated zero/invalid/encrypted attempts create12 workers and leave0 active, including4 password rejections. The encrypted fixture is independently decrypted with its known password to establish that it is a valid encrypted PDF. `test-results/pdf-inert-final/pdf-inert.json` and its screenshot were inspected. `verify-library-native.cjs` PASS: actual two windows prove foreign/stale context refusal and external-agent blur preservation, alongside concurrent GUI+Node mutation, exact revoked-lease/corrupt-byte preservation, interrupted PDF recovery, recipe cancellation/tree teardown and failed-note close/Cancel/retry bytes. Its real print PDFs retain scientific values42.5/23.75, one240×120pt figure page and previous outputs on failure. Artifacts: `test-results/library-native-context-final/`. An earlier isolated inert run encountered a denied read of the host dconf path; later native runs explicitly set memory GSettings/no session bus. No host dconf write succeeded or is claimed.

Existing `verify-snip-gui.mjs` after extraction: ALL PASS22. The real Reader capture produces a950×462 PNG with dark-pixel ratio0.1251,288dpi and matching provenance/sidecar; rename/dedup/Escape and ordinary Alt-drag remain intact. Artifacts: `test-results/snip-controller-final/`. Renderer check is0 errors/0 warnings and headless type check exits0; `git diff --check` is clean.

The only remaining conditional NR10 view split is Library markup: the plan explicitly requires stable query/mutation controllers first. World paging state, result selection/bulk actions and settings drafts still reside in LibraryMode with no shared stable controller API; controlled browser acceptance now protects their behavior, but does not manufacture that prerequisite. All explicitly requested Reader controllers and native families are extracted. No further renderer markup/CSS rearrangement is justified by line count.

Final broad rebuilt pure/UI/Reader/Paper/package gates belong to root and must run against these latest sources; earlier frozen cohort counts do not qualify this reopened wave. Remaining external qualifications are physical Windows/macOS and Wayland/NVIDIA, real institutional/publisher sessions, installed Chrome/Firefox capture/service-worker compatibility and a matching signed Firefox artifact. The stale signed XPI is not evidence for hardened source. No model/live paid service, signing, release or publication was performed. NR production/tests/ledger are now frozen; no owned browser/native child remains, and Xvfb:187 stays available for final integrated native/package work.
