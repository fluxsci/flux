# V0.2 Paper / Slide implementation ledger

Updated 2026-09-20/21, isolated branch `codex/v020-fortification`, worktree `/tmp/flux-v020-fortification`. Parent agent owns the engineering-guide entry, central package ledger, manifest registration, final frozen-tree suite and release decision. No release published. Linux Chromium tests use the worktree service on **:1421**, preserving the pre-existing **:1420** service. CLI tests execute via `/tmp/run-flux-isolated.py` (disposable HOME/config/cache); projects are disposable. No Lighttable code is involved.

## Implemented packages

| Package | Implementation and focused evidence |
|---|---|
| PS-01 | Keyed CodeMirror session per document; serialized navigation intent; outgoing document/comment save checks; generation-guarded reload, comments, DOI, title publication; original-path autosave ownership. `verify-paper-session-gui.mjs`: **7/7**, including A→B Undo, exact A/B saved bytes, delayed older navigation, and A's saved HTML while B is selected during an include read. |
| PS-02 | Shared durable compare-and-restore journal, exact originals/transforms, path confinement, durable write/remove directory sync, restore timestamps, recovery startup adapters, operation export leases and explicit ownership checks. Unique Quarto profiles are journal-owned generated resources; no fixed-profile clobber. `verify-v020-export-recovery.ts`: **5/5**, real SIGKILL after first rewrite, Unicode/CRLF originals, untouched include, original mtime, idempotent journal removal. In-memory conflict test retains unexpected newer bytes. |
| PS-03 | Immutable session/text/figure/bibliography render snapshot, bounded per-plot preparation, format capability refusal, checked flush/bibliography, shared DOCX postprocess and ZIP/XML/internal-relationship/marker validation, owned temporary artifacts then atomic publication. CLI/native Quarto runs from the document directory because Quarto rejects paths in `--output` and a different cwd changes dependency paths. `verify-v020-docx-publication.ts`: **9/9** against real Quarto DOCX and HTML; corrupt zero-exit artifact retains prior bytes, Zotero fault produces validated marker-free plain references and warning. `verify-paper-export.mjs`: **PASS**, replaced old source-wiring regex with actual compiler artifacts. |
| PS-04 | Checkout baselines scoped by slide and element; complete deck load reset; companion snapshot includes scoped baselines, endpoint/coalescing state; explicit history gesture owner for same-key typing runs. Cross-slide ID reuse preserved. `verify-v020-paper-slide.ts` **26/26**; `verify-w19-sharing.ts` **PASS**, including one undo per burst and a distinct sealed next gesture. |
| PS-05 | Animation mutators reject Design/exclusive-family conflicts before mutation; generated reveal provenance distinguishes authored tracks/phases; ghost phase owner remaps on duplication; only removed generated-track groups are pruned. `verify-slide-autobuild.ts`, `verify-slide-track-ops.ts`: **PASS**. |
| PS-06 | Destination-default deletion across retyping; punctuation-safe numeric tween token; authored dash restoration after drawOn; shared gradient paint/definitions through outline morphs; gradient-only closed shapes inflate correctly. `verify-slide-outline.ts`, `verify-slide-player.ts`, `verify-slide-exits.ts`, `verify-slide-scatter-showcase.ts`, `verify-slide-export-transform.ts`: **PASS**. `verify-slide-become-browser.ts`: **21 assertions PASS**. `verify-v020-slide-paint.ts`: **4/4**, PNG equality after forward/reverse traversal plus red/blue gradient pixels. |
| PS-07 | Portable payload scrubs authoring provenance, full notes retained/embedded notes omitted, disabled ghost identity retained, offline hashed-script CSP shared by Paper/Slide hosts. Direct HTML exporter also applies portable projection. `verify-slide-embed-core.ts`: **46/46**, including exact runtime hash. Worker browser gate validates intended pagination and blocks unknown inline script/external requests; paint gate directly checks private paths absent in exported HTML. |
| PS-08 | GUI owning editor refreshes/saves then exports saved snapshot; CLI `refreshSources:false` / `--saved` path prevents recursive human-lock conflict; structured export warnings reach GUI. `verify-slide-export-bridge.ts`: **PASS**. Registry flag mapping is SH-owned. |
| PS-09 | Shared protected-context scanner for long/type fences, leading YAML, comments, indented/inline code and math; consumers include figure embeds, include walk, export refs/alts and Zotero markers; real table grammar and first-valid duplicate numbering; shared math grammar moved to `src/lib/manuscript/mathGrammar.ts` with compatibility re-export; structural delimiter gates, snake_case inline rules. Table/refnum/mathgrammar/export/Zotero focused gates pass; final cross-consumer Paper suite remains root's acceptance. |
| PS-10 | Explicit annotation-only worker mode bypasses rescue; bounded pending work, per-editor cancellation on disable/switch/dispose; foreground work can run between independent rescue probes, preserving dictionary barriers; vocabulary candidate index and one occurrence scan per refresh. `verify-local-corrections.ts`: **117/117**. Browser worker gate: 17 unfamiliar scientific backlog terms then foreground **944.8 ms**; warm-worker editor update-to-painted-frame **6.4 ms**. These are the specified focused scenario, not universal native INP claims. |
| PS-11 | Shared safe no-trust KaTeX options, all-error source fallback, explicit expansion/source/output limits; lazy worker, duplicate coalescing, bounded queue/LRU bytes, 250 ms expression watchdog and restart; local render placeholders prevent overlapping job substitutions. `verify-paper-math.mjs`: **PASS**, valid inline/display/export/fonts/navigation. Worker gate: malformed internal exception + recursive macro preserve surrounding prose, valid equation and later export, **88.0 ms** including warm initialization in this run. Node-only logic tests use the same bounded KaTeX options; actual GUI export uses worker. |
| PS-12 | Ambient active/disposed lifecycle prevents restart from hidden-pane typing; margin pointercancel/hide/dispose cleanup; title/export/preset dialogs reuse shared modal-focus action; keyboard preset insertion/delete separation; async preset publication guard. Final focus/ambient suites are root-owned after freeze. |
| PS-13 | Exact per-plan numerical-station memoization only; at most 4096 cached points per immutable parameterization, no global cache, LUT, or changed tolerance. Six pre-change SHA256 correspondence/sample oracles (rounded rect, 200-node curve, line/ring, cusp/near-zero, reversed winding, large coordinates and gradient paint) match exactly. Measured preparation times **53.7/75.6/37.2/38.2/46.9/33.8 ms → 14.0/13.5/5.8/6.3/7.1/6.2 ms**. Microbenchmark proves those cases only; broad dense/native acceptance remains final-suite work. |
| PS-14 | Finite 48 kHz stereo silence clock defines exact timeline duration; trimmed/resampled delayed clips mix into that clock; deterministic limiter/final sample trim. Real AAC mux gate runs **20 repetitions**, decodes full sample count, checks finite RMS, known tone and final silence; **3 harness assertions PASS** after adding a two-tone concurrent/looped mux. Every PCM sample is finite, full coverage is mandatory, and trailing AAC padding is bounded below 1024 samples. Actual native 30/60 fps concurrent/looped capture passed; detailed final acceptance evidence is below. Broader native runs remain root-owned until their current-tree results are recorded. |

## Additional shared-contract work

Slide read/resolve now stages evidence and plot caches; only a still-current root/deck/tenant/edit generation can adopt them after the final I/O check. Saving serializes immutable deck/assets/manifests, captures persistence generation, checks expected-root lease ownership before publication, acknowledges dirty assets by matching generation and updates baseline only for the owning session. New-deck loading uses the same staged publication discipline. Manifest registration uses SH's fresh-manifest transaction. F08 morph source import installs the prepared plot only inside the validated target commit. Figure/Slide conversion completion is tracked separately by the root ledger.

## Evidence and remaining acceptance

Copied focused logs: `test-results/v020-paper-slide/`. Rendered artifacts: `test-results/out/v020-gradient.html` and `test-results/out/v020-gradient-mid.png`; the PNG was opened and visually inspected (smooth red→purple→blue closed intermediate, complete white stroke). Real DOCX/HTML fixture artifacts were parsed/read then disposable projects removed by the gate. No Word application rendering is claimed from ZIP validation.

`npm run check` latest focused result: **0 errors, 0 warnings**. Pure legacy fixtures were repaired to full valid manifests; terminal-animation tests now assert restoration of the actual authored solid stroke, and retyping asserts the exact destination's implicit opacity default. Numerical tolerances/performance budgets were not loosened. Source-regex-only export tests were replaced with artifact checks.

Pending parent acceptance: full frozen-tree `paper-gate`, full common verification/build, dense Figure/Paper/Slide performance and native media checks. Physical Wayland/NVIDIA display, macOS/Windows native execution, and actual Microsoft Word field behavior have not been exercised here and must not be reported as passed. No platform limitation is used to imply a failed test passed.

Isolation correction: an initial direct invocation of two scaffold-based CLI fixtures failed attempting the sandbox-protected real FluxLib lock directory. No library content write was reached; subsequent production-import checks all use the disposable environment wrapper. Parent retains the original checkout/config preservation comparison.

### F01/F03 conversion and Slide publication integration (2026-09-21)

Both GUI conversion directions now acquire expected-root project → slides leases, read a complete supported Figure snapshot, read required scientific asset bytes, preserve raw canvas/index metadata, validate before any asset write, and reject reused IDs with different bytes. Unsaved Figure assets are copied into the new deck. Slide → Figure publishes canonical files plus fresh manifest registration in one recovery generation and uses the existing reference coordinator. A failed conversion can leave only newly created, unindexed immutable asset files; it never overwrites an existing asset to resolve a collision.

`deckGeneration.ts` is a shared GUI/Node injected-IO publisher. GUI save, source refresh/reopen, direct/new deck publication, and Node save/mutate/export catch-up stage assets, semantic sidecars, deck and fresh manifest in the same durable journal. Leases follow project → slides → manifest; custom registered relative JSON paths remain supported. Acknowledged save baselines, dirty asset generations, cache bytes and accepted intrinsic dimensions publish only after persistence. Source preparation merges new user geometry; later edits remain dirty. Deferred target reads publish no cache or baseline. Live load adoption is serialized against saves.

Exact focused evidence (all under `/tmp/run-flux-isolated.py`):

- `verify-v020-conversion.ts`: **17 checks PASS**, actual filesystem reopen, both-direction missing/corrupt/future/EACCES/EBUSY sibling matrix, zero writes before invalid numeric refusal, original-byte preservation after manifest failure, metadata compatibility, asset collision refusal, stale-root refusal and direct deck evidence check.
- `verify-v020-slide-persistence.ts`: **8 checks PASS**, ENOSPC at SVG/sidecar/deck/manifest restores all accepted files and keeps model/cache/dirty state; edit during preparation retains position and correct scale; ordinary dirty asset save acknowledges only complete commit; read-only resolution is passive; custom registered relative deck JSON paths outside `slides/` retain their location and extension metadata.
- `verify-slide-source-sync.ts`: **31 assertions PASS** through the real Node export path. Repaired stale payload assertions to inspect source paths and accepted size baselines in saved authoring bytes, while asserting portable payload omission separately. Semantic-sidecar revision oracle now uses preserved `plotType`, not intentionally stripped custom authoring metadata.
- `verify-slide-decks.ts`, `verify-canvas-divergence.ts`, and `verify-v020-figure.ts`: **PASS** after adapter fixture supplies actual resident Slide ownership and required file-removal support.
- `npm run check`: **0 errors, 0 warnings** after direct-writer/adoption cleanup at `/tmp/ps-complete-check.log`.

The broad UI run exposed a real delayed-math table repaint gap: widget equality tracked worker initialization instead of each expression's completion. `mdInline.inlineSig` now tracks expression cache readiness; the targeted frozen Paper table gate now passes all 35 behavioral checks, including completion, hover, both jump routes, async math and source folding.

### Targeted frozen-host acceptance (2026-09-21)

- `verify-slide-source-sync-gui.mjs`: **32 assertions PASS**, `/tmp/ps-source-gui-frozen.log`. Corrected the integration with the new atomic mutation contract: accepted source facts use `mutateDisplay`, so source refresh adds no authoring Undo entry; existing Undo/Redo still restore author geometry with current accepted bytes and intrinsic sizes.
- `verify-slide-authoring-gui.mjs`: **27 assertions PASS**, `/tmp/ps-authoring-gui-frozen.log`. Cross-step group copying now copies a Change directly into the checked destination; it never creates an invalid temporary duplicate in the source step. Same-step and occupied-destination exclusivity remain enforced before mutation.
- `verify-paper-tables.mjs`: **all 35 checks PASS**, clean console, `/tmp/ps-tables-frozen.log`. Paper initial figure/bibliography loads now come from their revision subscriptions exactly once. `data-paper-sources-ready` reports completion of overlapping initial revisions while the editor stays responsive during loading; seed-based fixtures can wait for an observable condition.
- `verify-v020-geometry-gui.mjs`: **PASS**, `/tmp/ps-geometry-gui.log`. Actual mounted Canvas preview/commit matches at 0/30/45/90 degrees for rectangle, text, path and line, including flips, preserved text font size, pinned opposite line endpoint, exact Undo and flipped crop. Settled hover paints in **16.6 ms** (100 ms limit unchanged). Real two-pair terminal double-click authors exactly three pen nodes; root fixed Chromium pointer-event semantics. Crop fixture now declares the asset's intrinsic dimensions instead of silently testing ordinary resize. JSON coordinates and screenshots: `test-results/out/v020-geometry-gui.json`, `v020-geometry-resize-preview.png`, `v020-geometry-pen.png`.
- `verify-v020-svg-hosts.mjs`: initial **35 checks PASS**, actual Figure DOM, standalone SVG file, detached browser gallery, Paper preview iframe, portable Paper and Slide HTML. Loopback recorder calibration succeeds, then all corpus hosts produce zero requests and inert script/event sentinels. Saved SVG/HTML bytes and rendered PNGs were inspected. Artifact inspection exposed local-use presentation attributes lost during glyph expansion; root fixed this, and the gate now also requires exact green/magenta glyph paint in all five inline/vector hosts. Final tightened gate result will be recorded below. No native Electron or physical-GPU claim follows from these browser-host checks.

Final tightened `verify-v020-svg-hosts.mjs`: **40/40 PASS in 4.5 s**, `/tmp/ps-svg-hosts.log` (copied to `test-results/v020-paper-slide/`). All five vector/DOM hosts preserve the exact inherited local-use paints after shared normalization, and the original detached-gallery source remains inert. Final SVG/HTML artifacts and PNGs are retained under `test-results/out/v020-svg-*`.

Final focused reruns after shared manifest staging and direct-destination track-copy integration: `verify-v020-paper-slide.ts` **26/26 PASS**; `verify-v020-conversion.ts` **17/17 PASS**; `verify-v020-slide-persistence.ts` **8/8 PASS**. Final strengthened SVG host gate **45/45 PASS in 4.5 s**, including exact per-placement `:root` paint (independent of gradient colors), exact local-use glyph paint, preserved indexed demo content and a fully formed fixture. Logs `ps-paper-slide-final.log`, `ps-conversion-final.log`, `ps-slide-persistence-final.log`, and `ps-svg-hosts.log` copied into `test-results/v020-paper-slide/`. Product sources frozen; parent owns final typecheck/build and full frozen Paper/native/platform acceptance.

## Final acceptance audit and fixture repairs — 2026-09-21

The previous native fixture assumptions were repaired without weakening assertions. `sourceSyncFixture.ts` now derives `paper/notes.qmd` from the actual manifest and stages canonical Figure registration. A Node fixture audit reads the registered document and verifies its real embed. Paper native source-sync samples decoded image pixels for both updated live and unchanged frozen plots; it does not query nonexistent inline SVG children or relax CSP. Child temporary paths are scoped so an external source's ordinary neighbor stays outside the implicit temp grant.

The rotation fixture now writes an actual display matrix using input `-display_rotation 90`, independently asserts FFmpeg's matrix report, retains the **90×160** expectation, and checks the normalized red landmark against a manually rotated source. An isolated encoder reproduction passed with whole-frame RGB MAE **0.0743/255**. No production rotation change was required.

Calibrated HDR replaces the retagged SDR-white oracle. `verify-v020-hdr-media.cjs` passes actual PQ and HLG input transfer/linear-signal checks, all **36** output frames, 12 reference patches, strict ramp/highlight detail, display endpoints, BT.709 metadata, original-byte preservation and poster checks. Independent gamut/Hable/BT.1886 reference maximum observed error is **2/255**, below the fixed **5/255** limit. Fixture rationale and primary references: `scripts/fixtures/hdr-media/README.md`. MOV/MP4/PNG/JSON artifacts are retained in `test-results/slide-video-clips/hdr-calibration/`; the PQ poster and JSON reference values were inspected. This establishes software signal fidelity, not a physical display qualification.

`verify-slide-media-export.ts` **PASS** in **27.0s**, actual Electron on isolated Xvfb **:187**, Mesa software rendering, explicit test-only `--no-sandbox`. Run evidence `test-results/runs/2026-09-21T06-01-12-909Z-19/verify-slide-media-export.ts/attempt-1/`. All 30/60 fps frames and timestamps are checked for pause/restart and two concurrent clips, one looping; 440/880Hz measurements establish both mixed signals, natural non-loop end and continuing loop after wrap. Both frame rates decode **115,712** finite PCM samples for the **115,200** intended samples; the **512** trailing samples are within one AAC packet. Full decoded 30/60 audio is identical. Muted capture, canceled/corrupt-media old-target byte preservation and temporary cleanup remain green. Native video artifacts are retained in `test-results/slide-media/`.

`verify-v020-audio-graph.cjs` **PASS3**, 20 repeated pause/restart AAC jobs plus a concurrent two-tone loop job; exact sample bounds, finite signal, separate frequencies and silent holds. `verify-orphan-slide-sources.ts` **PASS20** after preserving the assertion on actual saved authoring bytes and separately asserting the explicit portable projection contains no private source-path keys. Focused logs are copied under `test-results/v020-paper-slide/` (`ps-hdr-calibration.log`, `ps-audio-graph-final.log`, `ps-orphan-final.log`).

| Required journey / package | Exact current gate and scope | Remaining final acceptance |
|---|---|---|
| PS01 document ownership, navigation, saved bytes | `verify-paper-session-gui.mjs`7; full Paper close/flush tests are SH/root-owned | Root frozen Paper cohort |
| PS02 compile/reopen recovery | `verify-v020-export-recovery.ts` real SIGKILL after first rewrite; export-prep compare/restore faults | Actual GUI matrix PASS7 below; root owns CLI matrix and final cohort |
| PS03 Word/PDF/static posters | DOCX publication9 actual Quarto/ZIP XML; Paper export UI and artifact tests | Run `verify-slide-embed-export.mjs` and `verify-slide-embed-electron.cjs` explicitly; `inline-slides` group does not include them. Actual Word UI and non-Linux checks remain unavailable |
| PS04–06/13 animation/history/scientific fidelity | Pure scoped-history and exact numerical/morph oracles; authoring27; source32; gradient rendered pixels | `slide-transforms`, `slide-ghosts`, full native video165frames/complex49frames and dense timing gates |
| PS07 offline portable content | SVG actual hosts45; private paths absent; hash CSP; authored-note policy | Real native data-movie and step0 export gates above |
| PS08 export owning editor | shared bridge/saved-option contract; actual saved decks retain authoring provenance | Full owning-GUI HTML export/native journeys, final registry/CLI gates |
| PS09 grammar/tables | tables35 and pure common grammar/export consumers | Full frozen Paper suite |
| PS10/11 typing with worker backlog | browser worker focused gate; native writer fixture now includes trusted input while actual math/correction jobs remain pending | Native focused PASS with ambient active and same/other-pane input; root exact frozen cohort still required |
| PS12 ambient/focus | lifecycle fixes and keyboard modal action | Frozen ambient/focus Paper gates |
| PS14 deterministic mux/fidelity | actual 30/60 media-export PASS above; HDR signal36frames/poster PASS; repeated audio21jobs PASS | Native media/import focused PASS; complete current-tree video/clips/source-sync cohort remains root-owned |

Native media import reached the updated real renderer-adapter duplication path and exposed a shared root-level journal fsync parent bug (`project.json` became `project.jso`). Root fixed the production helper and owns its durability regression. Native media/import then passed. The fixture now implements actual remove and directory-sync operations instead of omitting required adapter behavior. Earlier failed runs remain evidence, not successful qualification.

### Native acceptance closure — 2026-09-21

- `verify-slide-video-media-electron.cjs`: **PASS7.6s**, run `test-results/runs/2026-09-21T06-06-19-064Z-17/`. This actual Electron run includes MP4/ProRes normalization and preservation, scoped media copy/collision rollback/revocation, byte ranges, real native decode/seek/playback/audio, actual rotation matrix plus analytic orientation, calibrated PQ/HLG frames and posters, exact native-player clock for short/long audio, source-free CLI import, actual renderer duplication adapter, and offline Paper data-movie playback. Invalid fixtures gained required real remove/directory-sync adapters; they were not weakened.
- `verify-writer-latency-inp.mjs`: **PASS14.6s**, run `test-results/runs/2026-09-21T06-12-39-629Z-17/`. The test now uses the resident mounted shell rather than a stale dynamically imported shell instance. Actual trusted native keyboard events overlap 64 outstanding scientific correction jobs and 64 substantial uncached math jobs. All eight keys per condition are inserted and reach two animation frames within **33.7ms** (same pane), **32.7ms** (other Paper pane), and **33.1ms** (math, two panes). The ambient background is active during these phases. Each worker queue settles/cancels safely, later correction and valid math complete; unchanged warm ambient/correction p95 delta gates both measured **0ms**. JSON with per-key trusted/pending/paint evidence and native screenshot is in the run's `artifacts/native-worker-input.{json,png}`. Source changes elsewhere occurred during this diagnostic cohort, so it is not represented as the final exact-source qualification.
- `verify-v020-gui-export-recovery.cjs`: first focused **PASS7 in11.8s**, actual built renderer + main + preload + Quarto. Test barriers wrap completed native operations, and the rendering barrier observes actual Quarto output; no timing sleep selects a kill boundary. An actual successful GUI Word export provides the prior validated DOCX, including primary/include text and a raster figure fallback. Five process-group SIGKILL cases (journal fsynced, first source rewrite, Quarto rendering, first restore, restore complete) reopen through the actual GUI project-open path and require exact Unicode/CRLF/LF bytes, both source mtimes, removed journal, and identical prior DOCX bytes. A newer external include survives, recovery visibly refuses authoring with the journal retained, and a resolved subsequent reopen completes safely. Detailed per-phase journals, byte/mtime snapshots, logs, DOCX and screenshot are under `test-results/gui-export-recovery/`. The final strengthened rerun **PASS7 in11.7s** also decodes the Word fallback PNG and asserts the exact authored blue center pixel **[67,133,190,255]**. That saved PNG was opened and visually inspected.

No PS product-source edits were needed for rotation or HDR. The shared journal parent-path defect was fixed by root, and native fixture updates preserve all existing performance tolerances, every-frame checks and failure-target preservation assertions. Final full Paper, common, native, packaged and dense gates remain the parent coordinator's responsibility. Physical Wayland/NVIDIA interaction, actual Word UI behavior, macOS and Windows remain explicitly unqualified.

### Final acceptance-gap closure — 2026-09-21

The plan-to-test audit found concrete missing assertions, then closed them instead of treating neighboring green gates as proof:

| Contract | New evidence |
|---|---|
| PS10 indexed vocabulary preserves correction behavior | `verify-v020-paper-contracts.ts`: 20 exact comparisons against frozen pre-index `c289627` implementation, with 5,000/20,000-word vocabularies, scientific/Unicode/protected syntax and blocked pairs. |
| PS06 apply/diff across all element types | The same gate checks 320 source/destination/optional-property cases across all eight element types, including reverse reintroduction, identity and immutable inputs. |
| PS11 failed/stale math jobs and pressure | `verify-v020-worker-lifecycle.ts`: real service code with controlled worker failures, constructor failure/retry, stale replies/errors, duplicate-key coalescing, 500-entry and 8MiB limits, 140 jobs with exactly129 executed (active+128 queue). Found/fixed constructor failure leaving jobs stranded and stale correction-worker errors terminating a replacement. |
| PS12 lifecycle and actual keyboard controls | `verify-v020-paper-lifecycle.mjs` PASS55: actual mounted Title/Export/Preset dialogs, focus/Tab/Shift-Tab/Enter/Escape, hidden/destroyed ambient non-resurrection, repeated activation pacing, margin pointercancel and owner hide. Found/fixed Enter on Title Cancel saving instead. Resume measured16frames, one loop. |
| PS12 art continuity vs normal aging | `verify-margin-bg.mjs` PASS with original active performance thresholds intact. A separate same-clock real grip drag preserves every original canvas pixel; normal fading remains free to change ink counts during the original active drag. Observed rest16.4ms, drag18.4ms, spawn p95 5.5ms/max6.8ms. |
| PS06 real browser fidelity | `verify-v020-slide-paint.ts` PASS36: gradients and compound holes at 0/.25/.5/.75/1; actual curved-path `getTotalLength`, authored dash/cap, geometry and paint compared with independent fresh players after forward/reverse DrawOn→Change seeks. Saved hole/gradient PNGs opened and inspected. |
| PS13 actual first-preview benefit | `verify-v020-morph-startup.mjs` PASS: cold actual GUI, 12 slides, two 200-node Becomes plus rounded rectangle. Exact same paths from controlled pre-memo algorithm and cached code. Median first preview112.6→34.7ms (3.24×); every cached preview/first seek≤100ms. Six screenshots and JSON retained. |

PS03 now captures document/root/live-source text, accepted figure models/bytes/captions and accepted bibliography before asynchronous export preparation. Rendering uses this capture even if native watchers advance live Paper to a newer revision. Quarto's document YAML overrides profile YAML (confirmed with a disposable actual DOCX); shared `prepareExport` therefore redirects the registered bibliography inside the same durable temporary-source transaction. Additional/custom authored libraries retain their paths. New prepare regressions verify scalar/list/default/custom cases and exact Unicode/CRLF restoration. `npm run check`: 0 errors,0 warnings. The strengthened native gate adds held actual Quarto typing plus delayed figure/caption/bibliography revision assertions; its current-build result is pending below, not inferred from the earlier PASS7.

Focused logs are copied under `test-results/v020-paper-slide/`: `ps-contracts-final.log`, `ps-worker-lifecycle-final.log`, `ps-lifecycle-final.log`, `ps-margin-final.log`, `ps-paint-final.log`, `ps-morph-startup.log`, `ps-prep-final.log`, `ps-check-final-snapshot.log`. Artifacts: `test-results/out/v020-morph-startup.json`, `v020-morph-{baseline,cached}-{0,1,2}.png`, `v020-paper-lifecycle.png`, `v020-gradient-{0,250,500,750,1000}.png`, `v020-hole-{0,250,500,750,1000}.png`. Final frozen full Paper/common/native cohorts and newly rebuilt packages remain coordinator-owned; earlier archives are not evidence for these latest changes.

The worker lifecycle gate also delays an actual old-project `loadBib` read across a newer accepted load, then verifies the late completion cannot replace the captured root or exact bibliography bytes (`bibliographyLatestOwner:true`). This tests the loader generation guard directly rather than inferring it from export success.

The final shared-writer audit identified Paper autosave's unleased read/check/write window. Autosave and explicit Keep mine now wait for document export before acquiring the captured-root manuscript lease; after acquisition they validate ownership and capture current text/baseline, then assert the lease before writing. `verify-paper-session-gui.mjs` strengthened result **PASS12 in5.6s**: no baseline read before lease acquisition, expected-root identity, a concurrent cold writer's exact scientific bytes retained with conflict, live queued typing, and replacement only after explicit Keep mine; original A/B history and switched-document HTML artifact checks still pass. Evidence `test-results/v020-paper-slide/ps-session-lease.log`.

The new actual native control exposed a CRLF comparison mistake before any temporary source write: CodeMirror's LF text was compared directly with untouched CRLF disk bytes. The visible refusal and preserved project are retained in `test-results/gui-export-recovery/failed-projects/`, with `ps-gui-export-snapshot-crlf-failure.log`. Preflight now compares CodeMirror's newline representation while the journal still reads/restores exact raw originals and mtimes. The edited revision case explicitly expects existing CodeMirror LF serialization; untouched control and kill cases retain strict CRLF/LF byte assertions. The native render-kill boundary now uses a controlled real Quarto Lua barrier, removing a possible first-output/restore race.

### Final native export snapshot and recovery result — 2026-09-21

`verify-v020-gui-export-recovery.cjs` final strengthened run **PASS8 in16.4s**, actual rebuilt Electron main/preload/renderer and actual Quarto, sole performance slot on private Xvfb :187/Mesa. During a controlled Quarto filter hold, **24/24 trusted keyboard events** reached two painted frames within **50.9ms** (100ms threshold unchanged). Canonical temporary source remained export-owned; all later typed text saved after restoration with the editor's existing LF serialization. A delayed read let actual native watchers advance the live figure to red and caption to99.25 and replace the bibliography. The inspected saved DOCX still contains the captured blue figure pixel **[67,133,190,255]**, both old3.25 captions and “Captured Old Reference”; neither newer caption nor later typed text appears. Live screenshot visibly shows the red figure/new caption and later typed text, establishing the revision difference rather than inferring it from a mocked success.

The five real SIGKILL boundaries (durable journal, first rewrite, actual Quarto filter underway, first restore, restore complete) all reopen through production GUI project-open and restore exact Unicode/CRLF/LF bytes, original mtimes and the previous valid DOCX. Newer external include bytes survive with retained journal/visible refusal; resolved reopen completes idempotently. The fixture's earlier empty caption was invalid (`captions['']`); it now uses the real `__figure__` key and requires3.25 in the persisted index before launching, preserving the caption assertion. That failed run's log/artifacts remain retained, distinct from passing evidence.

Final artifact paths under `test-results/gui-export-recovery/`: `revision-revision-export.json` (per-key trusted timing and exact source evidence), `.json.docx` (actual final Word artifact), `.json.document.xml`, `.json.figure.png` (decoded raster fallback), `.json.png` (actual live red/new-caption/typed editor screenshot), `control-control-export.json.docx`, `prior-valid.docx`, and per-phase barrier/recovery JSON+logs. Saved DOCX ZIP/XML was parsed, flattened text inspected, and both blue fallback and live red screenshot opened. Log copied to `test-results/v020-paper-slide/ps-gui-export-snapshot-final.log`. GSettings uses memory and DCONF a private disabled profile. No actual Microsoft Word application behavior is inferred from these checks.

All PS-owned product/test/ledger files are now frozen for coordinator commit and final exact-source Paper/common/native cohorts. The authorized local acceptance gaps identified in the final audit are closed by the focused results above; full final cohorts, latest Linux package smoke, physical display qualification and unavailable non-Linux/Word UI checks remain explicitly coordinator/platform responsibilities.

### Frozen-cohort cell-edit scaling correction — 2026-09-21

The final scale cohort exposed a real margin regression: 20,000-line table-cell synchronous p95 **12.4ms**, control1.9ms (existing2ms denominator floor), ratio **6.2×**, exceeding the unchanged6× gate. An exclusive repeat passed narrowly at11.6ms/5.8×; both original results are retained. A120-edit Chromium CPU profile attributed240.6ms to protected-span construction and366.5ms to the four largest recursive Text.lineInner nodes. PS09's scan repeatedly searched all protected spans for every line and descended the immutable text tree for non-table lines.

The narrow correction is confined to `science/tableModel.ts:scanTables`: advance through ordered protected spans and `Text.iterLines` once, preserving UTF-16 offsets, and call the unchanged table parser only for unprotected pipe-bearing candidates. Table grammar and protection rules are unchanged. `verify-table-model.ts` now includes a frozen pre-optimization traversal oracle requiring exact models/offsets for **241 real tables and360 excluded protected pseudo-tables**, adjacent comments/fences/math, captions, absorbed prose, start/end boundaries and an edited immutable Text tree. The full model gate passes61 checks.

Three independent consecutive unchanged `verify-scale-paper.mjs` runs each **PASS7 in14.3s**. Table-cell heavy p95/ratio: **6.7ms/3.35×**, **7.6ms/3.80×**, **6.9ms/3.45×**; paint p95≤33.5ms. Prose/citation ratios also pass and prose triggers zero block-field rebuilds. Populations, sample counts, performance floors and thresholds were not changed. The existing giant single-paragraph diagnostic remains **189.3–190.3ms**, recorded-only; this fix does not claim that pathological Lezer parse cost is resolved.

Focused scientific/behavioral verification: `verify-paper-tables.mjs` **all35 checks PASS**, clean console; table-model61, refnums24, V020 Paper/Slide26, table-fold and change-gate all PASS. Evidence directory `test-results/v020-paper-slide/scale-diagnostics/`: `frozen-first.{json,log}`, `before-exclusive.{json,log}`, `cell-before.cpuprofile`, `ps-scale-profile.log`, `linear-{1,2,3}.{json,log}`, and focused table/grammar logs. No new verification script or registration is required. Only these two source/test files plus this ledger changed; they are frozen again for coordinator typecheck/build/full pure/Paper/scale/native qualification.
