# Flux — notes for agents

**See `AGENTS.md`** — the invariants live there, vendor-neutral, so every agent reads the same
file. Read it in full, then `docs/AGENT_ENGINEERING_GUIDE-RUNNING.md` as it instructs.

This stub exists only so Claude Code auto-loads the pointer; do not add rules here.
